# Backend Sources of Truth

## Purpose

This document identifies the single canonical owner for each backend responsibility. It is normative for new and changed code, while the **Current known mismatches** section records where the repository does not yet match the target convention.

“Allowed consumers” identifies which code may import or call the owner. It does not grant a consumer permission to take ownership of the responsibility itself.

## Ownership table

| Responsibility | Canonical owner | Allowed consumers | Forbidden duplication |
| --- | --- | --- | --- |
| Process entry point, server startup, shutdown, and process-level failure handling | `backend/index.js` | Package start scripts and the Node.js runtime | Additional server listeners, startup entry points, or process signal owners elsewhere |
| Express application instance and composition order | `backend/src/app.js` | `backend/index.js`; application-level test harnesses when introduced | Creating another production Express app or composing global application middleware in feature modules |
| Dotenv loading | `backend/src/config/index.js` | No other module loads dotenv | Loading dotenv in any other module or establishing multiple dotenv loaders |
| Reading environment variables | `backend/src/config/index.js` | No other module reads `process.env`; consumers use its exported configuration | Any direct `process.env` access in another module, including another module inside `src/config/` |
| Environment validation, parsing, normalization, and approved defaults | `backend/src/config/index.js` | Any backend module may consume the normalized exported values where appropriate | Secondary environment parsers, validators, normalizers, defaults, or config objects representing the same values |
| MongoDB/Mongoose connection lifecycle | `backend/src/config/mongodb.js` | Startup/shutdown owner; approved database tooling | Calling `mongoose.connect()` or owning connection lifecycle in routes, controllers, services, models, or new connection modules |
| One-time database migration invocation and connection orchestration | `backend/scripts/run-migration.js` | Explicit package scripts and operators executing an approved migration | Running migrations from application startup, request paths, seed files, or a second migration runner |
| Versioned migration data transformations | `backend/src/database/migrations/<versioned-name>.js` | Canonical migration runner and focused migration tests | Embedding migration transformations in services, models, startup, seeds, or parallel migration locations |
| Platform ExperienceLevel codes (V4 F06) | `backend/src/constants/experience-level.js` | ExperienceLevel persistence and dataset initialization | Inline redefinition of ExperienceLevel codes or expanding beyond the locked six-member set |
| ExperienceLevel persistence model | `backend/src/models/experience-level.model.js` | Approved migration tooling and future read consumers | A second ExperienceLevel schema/model, extra catalog metadata fields, or runtime CRUD/service ownership |
| ExperienceLevel canonical dataset initialization (V4 F06) | `backend/src/database/migrations/v4-experience-level-dataset.js` | Canonical migration runner and focused migration tests | Initializing ExperienceLevel from seeds, startup, request paths, or a second migration/seed owner |
| Cloudinary configured client and connection verification | `backend/src/config/cloudinary.js` | Startup owner and services that perform Cloudinary operations | Reconfiguring Cloudinary or creating another configured Cloudinary client elsewhere |
| SMTP/Nodemailer transport | `backend/src/config/mailer.js` | Mail service | Creating parallel SMTP transporters or reading SMTP environment variables directly instead of consuming `src/config/index.js` |
| Sending application email | `backend/src/services/mail.service.js` | Controllers and other services that coordinate an approved workflow | Direct `transporter.sendMail()` calls outside the mail service or additional general-purpose mail senders |
| Root API router | `backend/src/routes/index.js` | Express application | Additional root API router registries or mounting feature routers directly in the entry point |
| Feature endpoint declarations and middleware chains | `backend/src/routes/<name>.routes.js` | Root router | Defining the same feature endpoint in multiple routers; embedding business logic or database access in routes |
| HTTP request/response translation for a feature | `backend/src/controllers/<name>.controller.js` | Feature routes | HTTP handling in services/models/utils; database/model access or business workflow ownership in controllers |
| Feature business workflows and business validation | `backend/src/services/<name>.service.js` | Controllers and other approved services | Reimplementing the workflow in routes, middleware, controllers, utils, or parallel services |
| Mongoose schema, model, and persistence representation | `backend/src/models/<name>.model.js` | Services and approved database tooling | Duplicate schemas/models or model ownership in controllers, routes, middleware, or utils |
| Cross-cutting request processing | `backend/src/middlewares/<name>.js` | Express application and feature routes | Business workflows in middleware or duplicate middleware for an already-owned concern |
| Session-bound access authentication | `backend/src/services/authenticate-access.service.js` (validation); `backend/src/middlewares/authenticate-access.js` (HTTP extraction and request enrichment) | Protected feature routes and their middleware chains | Reimplementing access/session validity checks in controllers, routes, utils, or parallel auth middlewares |
| Company Manager onboarding access authentication | `backend/src/services/authenticate-access.service.js` (`authenticateOnboardingAccess`); `backend/src/middlewares/authenticate-onboarding-access.js` | V2 Company onboarding routes that require Company Staff Manager membership semantics with `PENDING_ACTIVATION` | Treating onboarding access as ACTIVE-account access or reimplementing onboarding auth outside the canonical owners |
| Company self-service GET/PATCH access composition | `backend/src/middlewares/authenticate-company-self-service-access.js` | Company GET/PATCH `/` routes | Parallel dual-mode auth for company self-service, or folding ACTIVE F09 access into onboarding auth |
| Session access for logout (ACTIVE or onboarding CM) | `backend/src/services/authenticate-access.service.js` (`authenticateSessionAccess`); `backend/src/middlewares/authenticate-session-access.js` | Logout route | Parallel logout credential checks outside the session-access owners |
| Platform Admin authorization | `backend/src/middlewares/authorize-platform-admin.js` | Platform-admin routes after access authentication | Role checks for Platform Admin workflows duplicated in controllers, routes, or services |
| Category level enum (`FIELD`, `POSITION`) | `backend/src/constants/category-level.js` | Category persistence and workflows | Inline redefinition of Category levels or a third level value |
| Category persistence model | `backend/src/models/category.model.js` | Category service and approved database tooling | A second `Category` schema/model, client-authored `normalizedName`, or direct use from routes/controllers/middleware |
| Platform Admin create Category FIELD (V4 F01) | `backend/src/services/category.service.js` (`createFieldCategory`) | Category / platform-admin controllers | Parallel FIELD-create workflows outside the category service; duplicating Platform Admin role checks in the service; accepting client `level`/`parentCategoryId` for FIELD creation |
| Platform Admin create Category POSITION (V4 F02) | `backend/src/services/category.service.js` (`createPositionCategory`) | Category / platform-admin controllers | Parallel POSITION-create workflows outside the category service; duplicating Platform Admin role checks in the service; allowing missing/non-FIELD parents; mutating the parent FIELD |
| Platform Admin account lock (V1 F10) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel account-lock workflows in auth or other services |
| Platform Admin account terminate (V1 F11) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel account-termination workflows in auth or other services |
| Platform Admin company registration review (V2 F04) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-registration list/detail workflows outside the platform-admin service |
| Platform Admin reject Company (V2 F06) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-reject workflows outside the platform-admin service |
| Platform Admin approve Company / TX-02 confirmation issue (V2 F05) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-approve or confirmation-issue workflows outside the platform-admin service |
| Platform Admin lock Company / TX-04 (V2 F10) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Company lock or CM termination-on-lock workflows outside the platform-admin service |
| Unmatched routes | `backend/src/middlewares/not-found.js` | Express application | Feature-specific or application-wide replacement 404 handlers elsewhere |
| Final centralized HTTP error handling | `backend/src/middlewares/error-handler.js` | Express application; errors forwarded by the request pipeline | Competing global error handlers or independently defined global error response contracts |
| Operational application error type | `backend/src/utils/app-error.js` | Routes, middleware, controllers, services, and other helpers as appropriate | Additional general-purpose application error classes representing the same contract |
| Generic reusable helpers | The relevant module under `backend/src/utils/` | Any layer for which the helper is dependency-safe | Feature business workflows, HTTP handling, or persistence ownership hidden in utils |
| Shared enum-like constants and fixed mappings | The relevant module under `backend/src/constants/` | Any backend layer that needs the value | Multiple constant modules defining the same domain concept or inline redefinition of canonical values |
| Platform Location vocabulary (V4 F03) | `backend/src/constants/location.js` | Any backend layer that needs Location values | A second Location member list, persisted Location catalog, Remote-as-Location member, or auto-synced/renamed administrative geography set |
| Platform EmploymentType vocabulary (V4 F04) | `backend/src/constants/employment-type.js` | Any backend layer that needs EmploymentType values | A second EmploymentType member list or persisted EmploymentType catalog |
| Platform WorkMode vocabulary (V4 F05) | `backend/src/constants/work-mode.js` | Any backend layer that needs WorkMode values | A second WorkMode member list, persisted WorkMode catalog, or treating `REMOTE` as a Location |
| Platform account types (`CANDIDATE`, `COMPANY_STAFF`, `PLATFORM_ADMIN`) | `backend/src/constants/user-role.js` | Any backend layer that needs platform roles | Inline redefinition of platform account types or restoring removed `COMPANY_MANAGER` on `User.role` |
| Company membership roles (`COMPANY_MANAGER`, `RECRUITER`) | `backend/src/constants/company-member-role.js` | Company membership persistence and workflows | Defining Company roles on `User.role` or a second Company-role owner |
| Company membership statuses (`ACTIVE`, `LOCKED`, `TERMINATED`) | `backend/src/constants/company-member-status.js` | Company membership persistence and workflows | A second Company-membership status owner |
| Job lifecycle statuses (`DRAFT`, `PENDING_APPROVAL`, `PUBLISHED`, `CLOSED`, `EXPIRED`) | `backend/src/constants/job-status.js` | Job persistence and workflows | Inline redefinition of Job statuses or inventing Product-excluded states such as `REJECTED` |
| V1/V2/V3 authentication-token types (`EMAIL_VERIFICATION`, `PASSWORD_RESET`, `COMPANY_APPROVAL_CONFIRMATION`, `RECRUITER_ACTIVATION`) | `backend/src/constants/auth-token-type.js` | Authentication-token persistence and workflows | Additional authentication-token type owners or unsupported authentication-token types |
| File storage operations | `backend/src/services/file.service.js` (`uploadFileBuffer`, `deleteFile`, `downloadFileBuffer`) | Controllers and approved services | Direct Cloudinary upload/delete/download operations in routes, controllers, middleware, models, or another general file service; treating temporary signed delivery URLs as public V7 CandidateCV access semantics or persisted canonical state; making restricted delivery the global default for every upload instead of an explicit caller option |
| User persistence model | `backend/src/models/user.model.js` | Services and approved database tooling | A second `User` schema/model or direct use from routes/controllers/middleware |
| Candidate self Profile read/update (V7 F01) | `backend/src/models/user.model.js` (existing persistence); `backend/src/services/candidate-profile.service.js` (Profile workflow and allowed-field enforcement) | Candidate Profile controllers after canonical access authentication | A separate CandidateProfile collection/model; Profile persistence outside User; direct User-model access from routes/controllers/middleware; allowing F01 to mutate email, role, status, password/authentication fields, sessions, or tokens |
| CandidateCV source types (`GENERATED`, `UPLOADED`) | `backend/src/constants/candidate-cv-source-type.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV source types or separate Generated/Uploaded root collections |
| CandidateCV statuses (`DRAFT`, `ACTIVE`) | `backend/src/constants/candidate-cv-status.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV statuses or inventing Product-excluded states such as `ARCHIVED` as `status` |
| CandidateCV visibility (`PRIVATE`, `PUBLIC`) | `backend/src/constants/candidate-cv-visibility.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV visibility or treating `PUBLIC` as effective anonymous/public access |
| Generated CV language proficiency (`BEGINNER`, `INTERMEDIATE`, `ADVANCED`, `FLUENT`, `NATIVE`) | `backend/src/constants/cv-language-proficiency.js` | CandidateCV Generated content persistence | A second proficiency vocabulary or free-form proficiency values outside the canonical enum |
| Application source (`DIRECT_APPLICATION`) (V9 S01) | `backend/src/constants/application-source.js` | Application persistence and approved Application workflows | Inline redefinition of Application sources or predeclaring `RECRUITER_INVITATION`, Source Recruiter, or Source Invitation before their owning version |
| Application statuses (V9: `APPLIED`, `WITHDRAWN`; V10 extends the approved vocabulary) | `backend/src/constants/application-status.js` | Application persistence and approved Application workflows | Inline redefinition of Application statuses or predeclaring statuses beyond their approved owning version |
| Direct Apply / PT-01 / TX-01 (V9 S02–S03 F01–F03) | `backend/src/services/application.service.js` (`directApplyToJob`, Generated/Uploaded snapshot capture helpers) | Candidate application controller after canonical access authentication and Candidate authorization | Parallel Apply workflows outside the application service; client-supplied Candidate identity; temporary application-only CV or combined upload+apply endpoint; Default CV auto-apply; Job/Company/CandidateCV/Recruitment Team mutation on Apply; duplicate `companyId` or recruiter assignment fields; inventing Replace/Withdraw/My Applications from Apply |
| Application persistence model, current Submitted CV Snapshot representation, local state matrices, revision, Candidate–Job uniqueness, and V10 current Assignee representation | `backend/src/models/application.model.js` | Application services and approved database tooling | A second Application schema/model; separate snapshot/history collections; duplicate `companyId`; `sourceRecruiterCompanyMemberId`, Invitation fields, assignment/status history, Job snapshot, Application-based Job deletion state or guard, or direct model use from routes/controllers/middleware |
| V10 Application persistence foundation (S01) | `backend/src/constants/application-status.js`; `backend/src/models/application.model.js` | Approved Application workflows and database tooling | Creating a parallel Application model/constants owner; adding `sourceRecruiterCompanyMemberId`, Invitation source, history/KPI/workload persistence, Job snapshot, or Application↔Job delete transaction not required by V10 |
| V10 V9 compatibility + Job-retention compatibility (S02; F01, F09) | Existing V9 Application workflows in `backend/src/services/application.service.js` (`directApplyToJob`, `replaceSubmittedCv`, `withdrawApplication`); V5 Job lifecycle retention in `backend/src/services/job.service.js` (`closePublishedJob`, `expirePublishedJobIfDue`, `deletePrePublicationJob` / pre-publication delete authority) | Candidate application controllers and Job lifecycle controllers | Reimplementing Apply/Replace/Withdraw outside the application service; inventing Application-existence Job delete guards or Apply↔Delete transactions; cascading Application deletes or nulling `Application.jobId`; auto-mutating Application when Job becomes `CLOSED`/`EXPIRED`; allowing Candidate Replace/Withdraw outside exact `APPLIED`; clearing Assignee on Withdraw; adding Assign/Pipeline/My Applications/projections from compatibility work |
| V10 Unassigned Applications + Primary Application View (S03; F01) | `backend/src/services/application.service.js` (`listPrimaryJobApplications`, Unassigned derivation via absent/null `assignedRecruiterCompanyMemberId`) | Job routes after canonical access authentication and Recruiter authorization (`GET /api/jobs/:jobId/applications`) | Parallel Primary Application list/read outside the application service; treating Unassigned as an `Application.status`; granting Supporting/Company Manager/Platform Admin/Candidate Primary view; expanding Application read into CandidateCV My CVs library access; mutating status/assignee/snapshot/identity on read; inventing Assign/Reassign/Take over, Pipeline, Managed Jobs aggregates, workload counters, or My Applications from this slice |
| V10 First Assign Application (S04; F02) | `backend/src/services/application.service.js` (`firstAssignApplication`; Unassigned→Assigned CAS via `version` + null/missing assignee; assignee eligibility + ACTIVE membership TX-02 acquire at commit) | Job routes after canonical access authentication and Recruiter authorization (`POST /api/jobs/:jobId/applications/:applicationId/assign`) | Parallel First Assign outside the application service; Supporting self-claim; trusting client company/team/authority fields; mutating status/snapshot/identity on Assign; auto `APPLIED → SCREENING`; Unassign / Assignment History / Reassign / Take over / forced reassignment / Pipeline / My Applications from this slice |
| V10 Reassign / Take over Application (S05; F03) | `backend/src/services/application.service.js` (`reassignApplication`; Assigned A→B CAS via `version` + expected assignee + non-terminal status; Take over = Reassign onto current Primary; shared assignee eligibility + ACTIVE membership TX-02 acquire at commit) | Job routes after canonical access authentication and Recruiter authorization (`POST /api/jobs/:jobId/applications/:applicationId/reassign`) | Parallel Reassign/Take over outside the application service; Supporting self-reassign/self-takeover/claim; Unassign or `A→null→B` intermediate; mutating status/snapshot/identity on Reassign; persisting takeover flags/history; forced reassignment / Pipeline / My Applications / workload counters from this slice |
| V10 Administrative Forced Reassignment (S06; F04, F09 partial) | `backend/src/services/application.service.js` (`forceReassignApplication`; CM-only public recovery A→B CAS when current Assignee is operationally ineligible; shared target eligibility + TX-02 acquire at commit; no Job-status gate for CLOSED/EXPIRED) | Job routes after canonical access authentication and Company Manager authorization (`POST /api/jobs/:jobId/applications/:applicationId/force-reassign`) | Parallel forced reassignment outside the application service; allowing public CM force-reassign while Assignee remains eligible via client-declared reason; making CM the Assignee; granting CM First Assign/Reassign/Take over/Pipeline; Unassign/`A→null→B`; persisting forced flags/history/audit; full LOCK/TERMINATE/team-removal orchestration; Pipeline / My Applications / workload counters from this slice |
| V10 Assignment/handoff lifecycle boundary foundation (S07 corrective; F04/BR-15/BR-28/BR-37/TX-02/PI-23) | `backend/src/services/application.service.js` (`executeTrustedPreLifecycleApplicationHandoff` + shared administrative handoff / First Assign / Reassign TX-02 membership acquire); reuses `acquireActiveRecruiterMembershipForTeamResponsibilityTx` from `job.service.js` | Trusted internal lifecycle/team orchestration callers (not a public HTTP surface); public CM recovery remains `force-reassign` | Exposing pre-lifecycle handoff on public CM API via client reason; full LOCK/TERMINATE/team-removal orchestration; final zero-active-responsibility Application guard; enumerating/transferring all Job-team + Application responsibilities; Unassign / history / `lifecycleOperationId` / workload counters / Pipeline / My Applications from this corrective foundation |
| V10 Recruiter LOCK/TERMINATE unified responsibility handoff (S08; F04/F09 partial; BR-28/TX-02/TX-05/PI-24) | `backend/src/services/recruiter.service.js` (`lockRecruiter` / `terminateRecruiter` via shared `executeUnifiedResponsibilityHandoffBeforeLifecycleCompletion`); Application lookup/guard in `application.service.js`; reuses V6 Job forced-transfer + Slice 07 trusted pre-lifecycle handoff | Existing Company Recruiter lock/terminate routes after CM authorization (`transfers[]` replacement context unchanged) | Inventing Application replacement heuristics outside transfer map / Supporting→Primary Take-over context; team-removal orchestration (Slice 09); Company-lock Application behavior; Pipeline / Managed Jobs / My Applications / workload counters; Assignment/Status/lifecycle history; persisted responsibility counters; global all-or-nothing Recruiter transaction |
| Replace Submitted CV / PT-02 / BR-30 (V9 S04) | `backend/src/services/application.service.js` (`replaceSubmittedCv`) | Candidate application controller after canonical access authentication and Candidate authorization | Reimplementing Replace workflow outside `application.service.js`; duplicating `replaceSubmittedCv` in routes/controllers/middleware; adding snapshot history/logs; mutating Application business identity fields; speculative downstream pipeline persistence |
| Withdraw Application / PT-03 / BR-03 (V9 S05) | `backend/src/services/application.service.js` (`withdrawApplication`) | Candidate application controller after canonical access authentication and Candidate authorization | Reimplementing Withdraw workflow outside `application.service.js`; duplicating `withdrawApplication` in routes/controllers/middleware; adding snapshot history/logs; mutating Application business identity fields; speculative downstream pipeline persistence |
| CandidateCV persistence model (V7) | `backend/src/models/candidate-cv.model.js` | Candidate CV services and approved database tooling | Separate Generated/Uploaded collections; Location/EmploymentType/WorkMode collections; Company/CompanyMember/Job/Application/JobInvitation ownership or references on CandidateCV; speculative Candidate Search indexes; treating `archivedAt` as a `status` value |
| CandidateCV My CVs list/detail (V7 F02) | `backend/src/services/candidate-cv.service.js` (`listOwnActiveCandidateCvs`, `getOwnActiveCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel My CVs read outside the candidate-cv service; including archived CVs in the active library; authorizing via `PUBLIC` visibility, Company, Recruiter, Platform Admin, anonymous, or URL-knowledge access |
| CandidateCV create Generated Draft (V7 F03) | `backend/src/services/candidate-cv.service.js` (`createGeneratedDraftCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Generated-Draft create outside the candidate-cv service; accepting client ownership/Company/Recruiter/Job fields; requiring ACTIVE completeness at create; synthesizing Harvard content from metadata; auto-activating or setting Default on create |
| CandidateCV Generated Builder save + completeness (V7 F04 Builder) | `backend/src/services/candidate-cv.service.js` (`saveOwnGeneratedContent` / `saveOwnGeneratedDraftContent`, `evaluateGeneratedCvCompleteness`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Builder save outside the candidate-cv service; requiring every incomplete Education/WorkExperience/Project to be complete for DRAFT save; auto `DRAFT → ACTIVE` from Builder save alone; syncing Profile or metadata from Generated content |
| CandidateCV Generated activation + ACTIVE lifecycle (V7 F04 activation) | `backend/src/services/candidate-cv.service.js` (`activateOwnGeneratedCandidateCv`, `saveOwnGeneratedContent`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel activation/ACTIVE-edit outside the candidate-cv service; activating incomplete content; auto-activating on completeness without explicit activate; leaving `ACTIVE` with incomplete content; persisting `DRAFT` + `isDefault=true`; inventing Default set/switch/unset, archive, upload, Preview/PDF, or Profile/metadata mutation from activation/ACTIVE edit |
| CandidateCV Uploaded PDF business validation (V7 F05/F06) | `backend/src/services/candidate-cv-uploaded-pdf.service.js` (`inspectUploadedCandidateCvPdf`); limits in `backend/src/constants/candidate-cv-uploaded-pdf.js` | Candidate CV upload/replace workflows | Putting Uploaded-CV PDF rules (actual PDF, password, 10 MB, 20 pages) into generic `file.service.js` or trusting client `mimeType`/size/page metadata alone; relying on global `MAX_FILE_SIZE_MB` as the Uploaded-CV size authority |
| CandidateCV create Uploaded CV (V7 F05) | `backend/src/services/candidate-cv.service.js` (`createUploadedCandidateCv`); Uploaded storage policy in `backend/src/constants/candidate-cv-uploaded-storage.js` | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Uploaded-CV create outside the candidate-cv service; persisting `UPLOADED/DRAFT`; accepting client ownership/Company/Recruiter/Job/storage metadata; creating CandidateCV before PDF validation succeeds; storing Uploaded PDFs with public Cloudinary `upload` delivery; inventing replace/Default/archive/Preview/Harvard workflows on create |
| CandidateCV replace Uploaded PDF (V7 F06) | `backend/src/services/candidate-cv.service.js` (`replaceOwnUploadedCandidateCvPdf`); PDF inspection via `inspectUploadedCandidateCvPdf`; Uploaded storage policy in `backend/src/constants/candidate-cv-uploaded-storage.js` | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Uploaded-PDF replace outside the candidate-cv service; mutating ownership/`sourceType`/`status`/`name`/`visibility`/Category/ExperienceLevel/preferredLocations/skillTags/employmentTypes/workModes/`isDefault`/`archivedAt` on replace; committing mixed old/new `uploadedFile` metadata; replacing before PDF validation; cleaning up the old external file before persistence succeeds; rolling back a committed current-file swap when old-file cleanup fails; deleting a storage artifact that is still the CandidateCV current file after a concurrent/stale replace; storing replacement PDFs with public Cloudinary `upload` delivery; inventing Uploaded file history/`previousFiles`/versioning, Default/archive/Preview/rename, or Generated replace |
| CandidateCV rename / metadata / visibility update (V7 F07) | `backend/src/services/candidate-cv.service.js` (`updateOwnCandidateCvMetadata`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel metadata-update workflows outside the candidate-cv service; rewriting Generated Harvard content or Uploaded PDF from metadata; mutating `sourceType`/`status`/`isDefault`/`archivedAt`/ownership/Company/Job fields; inventing derived public-access fields (`isSearchable`, `effectiveVisibility`, `isPubliclyAccessible`); treating `PUBLIC` as anonymous/Recruiter/Company Manager/Platform Admin access or Candidate Search in V7; auto-activating/deactivating Generated CVs from metadata; creating catalog collections or Skill Catalog ownership in V7 |
| CandidateCV fixed Harvard PDF rendering (V7 F08) | `backend/src/services/candidate-cv-harvard-pdf.service.js` (`renderHarvardCandidateCvPdf`); section vocabulary in `backend/src/constants/harvard-cv-section.js`; embedded Unicode fonts under `backend/assets/fonts/noto-serif/` | Candidate CV preview/official-download workflows | Parallel Harvard/PDF renderers outside this owner; multiple templates; Canva/free-layout/AI rendering; persisting Generated PDF artifacts, versions, or history as canonical CandidateCV state; lossy ASCII/`?` sanitization of valid Unicode `generatedContent` to work around StandardFonts WinAnsi limits |
| CandidateCV Preview / Download (V7 F08) | `backend/src/services/candidate-cv.service.js` (`previewOwnCandidateCv`, `downloadOwnCandidateCv`); Generated PDF via `renderHarvardCandidateCvPdf`; Uploaded bytes via `file.service.js` (`downloadFileBuffer`) using Candidate Uploaded storage policy | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Preview/Download outside the candidate-cv service; authorizing via `PUBLIC`, anonymous, Recruiter, Company Manager, Platform Admin, or URL knowledge; returning Cloudinary/public URLs as the V7 access model; exposing `uploadedFile.storageKey` in Candidate-facing HTTP serialization; fetching Uploaded PDFs as public `upload` delivery when the domain policy is restricted; persisting `publicUrl`/`publicDownloadUrl`/`isPubliclyAccessible` or Generated PDF state; official PDF download of Generated `DRAFT`; Preview that mutates `status`/completeness/metadata/visibility/Default/archive; inventing Default/archive/Builder/upload/replace from Preview/Download |
| CandidateCV Default management / TX-01 (V7 F09) | `backend/src/services/candidate-cv.service.js` (`setOwnCandidateCvAsDefault`, `unsetOwnCandidateCvDefault`); maximum-one via CandidateCV partial unique index on active Default | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Default workflows outside the candidate-cv service; adding `User.defaultCandidateCvId` or Default history; auto-selecting Default on create or after unset/loss of eligibility; setting Default on Generated `DRAFT`, archived, foreign, or non-ACTIVE CVs; non-atomic switch that can leave zero Defaults mid-failure; inventing Archive (F10), Apply-with-Default, Candidate Search, Application, or Job Invitation from Default management |
| CandidateCV Archive (V7 F10) | `backend/src/services/candidate-cv.service.js` (`archiveOwnCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Archive outside the candidate-cv service; hard-deleting CandidateCV or external Uploaded PDF on archive; inventing `status=ARCHIVED`, restore/unarchive, archive history/version collections; auto-selecting Default replacement; mutating ownership/`sourceType`/`status`/`visibility`/metadata/`generatedContent`/`uploadedFile` beyond atomic `archivedAt` + `isDefault=false`; leaving `archivedAt != null` with `isDefault=true`; inventing Candidate Search, Application snapshot, or Job Invitation from Archive |
| Company persistence model | `backend/src/models/company.model.js` | Services and approved database tooling | A second `Company` schema/model or direct use from routes/controllers/middleware |
| CompanyMember persistence model | `backend/src/models/company-member.model.js` | Services and approved database tooling | A second `CompanyMember` schema/model or direct use from routes/controllers/middleware |
| Job persistence model (V5–V6) | `backend/src/models/job.model.js` | Job service and approved database tooling | A second `Job` or `RecruitmentTeam` schema/model, duplicating Recruitment Team persistence outside the Job schema, replacement V4 catalog collections, or direct use from routes/controllers/middleware |
| Job Recruitment Team persistence and read boundary (V6 F01) | `backend/src/models/job.model.js` (embedded team fields); `backend/src/services/job.service.js` (team-read workflow) | Job controllers and approved database migration tooling | A separate RecruitmentTeam collection/model; a parallel team-read workflow outside the Job service; reusing generic V5 Job-content visibility to grant Supporting Recruiters broader content access; speculative audit/history/notification persistence |
| Recruiter create Job DRAFT (V5 F01) | `backend/src/services/job.service.js` (`createDraftJob`) | Job controllers | Parallel Job-create workflows outside the job service; treating client `companyId` as tenant authority; requiring submit-complete content at create time |
| Recruiter edit Job DRAFT (V5 F02) | `backend/src/services/job.service.js` (`updateDraftJob`) | Job controllers | Parallel DRAFT-edit workflows outside the job service; mutating ownership/creator/Primary/status/`publishedAt` through content edit; allowing non-Primary or non-DRAFT content mutation |
| Internal Job visibility boundary (V5 F03) | `backend/src/services/job.service.js` (`buildInternalJobVisibilityFilter`, `listInternalJobs`, `getInternalJob`) | Job controllers and later Company Staff Job workflows that must reuse the same visibility scope | Parallel internal Job list/detail authorization outside the job service; authorizing via creator or historical Primary alone; treating Job id as tenant authority; expanding into Candidate/public discovery semantics |
| Recruiter submit Job DRAFT for approval (V5 F04) | `backend/src/services/job.service.js` (`assertJobReadyForApprovalLifecycle`, `submitDraftJob`) | Job controllers and later approve/publish revalidation | Parallel submit workflows outside the job service; applying submit completeness at create/edit time; mutating content/ownership/creator/Primary during submit; non-atomic status transition that can leave partial lifecycle state |
| Company Manager Job approval-decision authority (V5 F05 / BR-20) | `backend/src/services/job.service.js` (`assertCompanyManagerJobApprovalAuthority`) | Later approve/publish and reject workflows (F06/F07) | Parallel CM approval-authority checks outside the job service; inventing review/marker state (`UNDER_REVIEW`, `reviewedAt`, approval-request entities); duplicating the F03 internal Job read path solely for pending review |
| Company Manager approve and publish Job / TX-01 (V5 F06) | `backend/src/services/job.service.js` (`approveAndPublishJob`, `assertJobPrimaryRecruiterValidForLifecycle`, `assertJobReadyForApprovalLifecycle`) | Job controllers | Parallel approve/publish workflows outside the job service; introducing an intermediate `APPROVED` state or separate publish step; mutating content/ownership/creator/Primary on approve; persisting `PUBLISHED` without `publishedAt` or `publishedAt` while still `PENDING_APPROVAL` |
| Company Manager reject pending Job / TX-04 (V5 F07) | `backend/src/services/job.service.js` (`rejectPendingJob`) | Job controllers | Parallel reject workflows outside the job service; inventing persisted `REJECTED` / rejection-metadata / soft-delete fields; deleting Jobs that have left `PENDING_APPROVAL`; cascading deletes into Company, CompanyMember, Category, or ExperienceLevel |
| Lifecycle-specific pre-publication Job hard-delete / TX-04 (V5 F12) | `backend/src/services/job.service.js` (`deletePrePublicationJob`, `assertPrePublicationDeleteAuthority`) | Job controllers | Parallel manual-delete workflows outside the job service; inventing `DELETED` / `isDeleted` / `deletedAt` / soft-delete fields; hard-deleting `PUBLISHED`/`CLOSED`/`EXPIRED`; granting Company Manager `DRAFT` hard-delete; granting Recruiter hard-delete outside own current-Primary `DRAFT`; authorizing delete via historical creator/former-Primary alone; cascading deletes into Company, CompanyMember, Category, or ExperienceLevel; duplicating a second persistence abstraction solely to distinguish reject from manual delete |
| Company Manager reassign Job Primary Recruiter / TX-03 (V5 F08) | `backend/src/services/job.service.js` (`reassignPrimaryRecruiter`, `assertCompanyManagerPrimaryReassignmentAuthority`, `assertRecruiterMembershipValidForJobPrimary`) | Job controllers | Parallel Primary-reassignment workflows outside the job service; mutating creator/company/content/status/`publishedAt`; reassigning non-effectively-`PUBLISHED` Jobs; assigning cross-tenant or inactive Primary; authorizing via creator/former-Primary/Job id/client `companyId`; mutating CompanyMember or inventing Primary-history / Supporting Recruiter documents |
| Manual close published Job / TX-02 (V5 F09) | `backend/src/services/job.service.js` (`closePublishedJob`, `assertManualCloseJobAuthority`) | Job controllers | Parallel close workflows outside the job service; mutating ownership/creator/Primary/`publishedAt`/content on close; closing non-effectively-`PUBLISHED` Jobs; hard-deleting or reopening `CLOSED`; authorizing via creator/former-Primary/peer Recruiter/Job id/client `companyId`; inventing `closedAt` or Job-history documents |
| Job effective expiration + public eligibility (V5 F10, F11 / TX-02) | `backend/src/services/job.service.js` (`resolveEffectiveJobStatus`, `isJobEffectivelyPublished`, `isJobPubliclyEligible`, `expirePublishedJobIfDue`, `findOutstandingPrimaryResponsibility`) | Later public/Candidate workflows and Job lifecycle processing that must reuse the same deadline semantics | Parallel deadline/effective-state or public-eligibility rules outside the job service; treating persisted `PUBLISHED` as still recruiting after deadline; inventing scheduler/cron/TTL as the sole correctness path; implementing Candidate discovery/search/Application solely to host the predicate; hard-deleting or reopening `EXPIRED`; mutating ownership/creator/Primary/`publishedAt`/content on expire |
| Recruiter outstanding Primary Job responsibility guard (V5 BR-41) | `backend/src/services/job.service.js` (`assertNoOutstandingPrimaryResponsibility`, `findOutstandingPrimaryResponsibility`) | Recruiter lock/terminate workflows | Reimplementing the outstanding-Primary query outside the job service; treating persisted `PUBLISHED` past deadline as still blocking; expanding TX-04/TX-05 into a jobs↔company_members multi-document transaction solely for this guard |
| Company membership / Company-role resolution (V3 F01) | `backend/src/services/company.service.js` (`resolveCompanyStaffMembership`, `findCompanyManagerMembership`, `resolveOwnedCompany`) | Auth, company, platform-admin, and approved middleware consumers | Parallel Company-role or tenant-membership resolution outside the company service |
| Company Staff tenant resolution and business authorization (V3 F02, F14, F15) | `backend/src/services/company.service.js` (`resolveCompanyStaffTenant`, `assertCompanyStaffBusinessAccess`, `resolveCompanyStaffBusinessContext`, `resolveCompanyManagerRecruiterManagementContext`, `resolveRecruiterBusinessContext`) | Approved middleware and Company Staff / Recruiter business services | Parallel tenant derivation from client `companyId`/`managerUserId`, or reimplementing layered business-access gates outside the company service |
| Company Staff business-access HTTP authorization | `backend/src/middlewares/authorize-company-staff-business-access.js` | Company Staff business routes after access authentication | Duplicating F14 layered checks in controllers, routes, or ad-hoc middleware |
| Company Manager recruiter-management HTTP authorization | `backend/src/middlewares/authorize-company-manager-business-access.js` | Company Manager Recruiter-management routes after access authentication | Duplicating BR-06/BR-24 role gates outside the approved middleware/service owners |
| Recruiter business-access HTTP authorization (V5) | `backend/src/middlewares/authorize-recruiter-business-access.js` | Recruiter Job routes after access authentication | Duplicating Recruiter role/tenant gates outside the approved middleware/service owners |
| Company Manager create Recruiter / TX-01 + activation issuance (V3 F03, F04) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel Recruiter-create or activation-token issuance workflows outside the recruiter service; including SMTP in TX-01; exposing bootstrap password or raw activation token to Company Manager |
| Company Manager list/detail Recruiter (V3 F08, F09) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel Recruiter list/detail workflows outside the recruiter service; authorizing from client `companyId`/`managerUserId`; exposing passwordHash, token, or session credential fields |
| Company Manager initiate Recruiter password reset (V3 F07) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel CM-initiated Recruiter password-reset issuance outside the recruiter service; creating a CM-specific token type; exposing raw reset tokens or passwords to Company Manager; treating password reset as unlock/restore |
| Company Manager lock/unlock Recruiter (V3 F11, F12 / TX-04; V6/V10 responsibility handoff) | `backend/src/services/recruiter.service.js` (`lockRecruiter` / `unlockRecruiter`; V10 extends lock with Job-team + Application unified handoff before completion) | Recruiter controllers | Parallel Recruiter lock/unlock workflows outside the recruiter service; changing User.status or Company lifecycle; restoring revoked AuthSession on unlock; unlocking platform-restricted Users or TERMINATED membership; completing lock while outstanding Primary Job or non-terminal Application responsibility remains |
| Company Manager terminate Recruiter / TX-05 (V3 F13, F16; V6/V10 responsibility handoff) | `backend/src/services/recruiter.service.js` (`terminateRecruiter`; V10 extends terminate with the same unified Job-team + Application handoff/final guard as lock) | Recruiter controllers | Parallel Recruiter termination outside the recruiter service; hard-deleting User/CompanyMember; changing User.status or Company lifecycle; releasing email for reuse; restoring TERMINATED membership to ACTIVE/LOCKED; completing terminate while outstanding Primary Job or non-terminal Application responsibility remains |
| Candidate/Recruiter self password-reset issue and TX-03 completion (V1 + V3 F06) | `backend/src/services/auth.service.js` | Auth controllers; Recruiter service for shared issuance | Parallel PASSWORD_RESET issuance/completion outside the auth service; changing User.status, CompanyMember, or Company lifecycle during reset |
| V3 TX-07 Company Manager → Company Staff cutover | `backend/src/database/migrations/v3-tx07-company-manager-to-company-staff.js` | Canonical migration runner and focused migration tests | Embedding TX-07 transformation in services, models, startup, or seeds |
| Company Manager registration / TX-01 onboarding (V2 F01) adapted by V3 TX-06 | `backend/src/services/auth.service.js` | Auth controllers | Parallel Company Manager registration workflows outside the auth service |
| Company Manager confirm approval / TX-03 activation (V2 F07) | `backend/src/services/auth.service.js` | Auth controllers | Parallel confirmation-consume or Company/CM activation workflows outside the auth service |
| Recruiter activation completion / TX-02 (V3 F05) | `backend/src/services/auth.service.js` | Auth controllers | Parallel Recruiter-activation-consume workflows outside the auth service; changing User.status, CompanyMember, or Company lifecycle during activation; exposing raw activation tokens |
| Company draft profile and tenant ownership (V2 F02) | `backend/src/services/company.service.js` | Company controllers | Parallel Company draft/tenant resolution workflows outside the company service |
| Company submit and immutable review snapshot (V2 F03) | `backend/src/services/company.service.js` | Company controllers | Parallel Company submit/snapshot workflows outside the company service |
| Company Manager resend approval confirmation (V2 F08) | `backend/src/services/company.service.js` | Company controllers | Parallel approval-confirmation resend workflows outside the company service |
| Company Manager active Company profile management (V2 F09) | `backend/src/services/company.service.js` | Company controllers | Parallel active Company profile get/update workflows outside the company service |

## Consumption rules

- Consumers import the canonical owner; they do not reconstruct its state or configuration.
- A convenience re-export may expose an owner, but it does not become a second owner.
- Constants representing one domain concept have one canonical module.
- Services may access models directly under the current architecture.
- A repository/data-access layer must not be introduced unless explicitly approved as an architecture change.
- When an ownership decision changes, this document and the architecture documentation must change with it.

## Current known mismatches

The following items describe the audited repository. They are not target patterns and do not propose a remediation.

### The model barrel is disconnected and empty

[`backend/src/models/index.js`](../../backend/src/models/index.js) exports an empty frozen object. Concrete model modules such as [`backend/src/models/user.model.js`](../../backend/src/models/user.model.js) are imported directly by services rather than through the barrel.

Whether model barrels should be mandatory, optional, or removed is not established by the target conventions. The mismatch recorded here is that the existing barrel presents no usable model ownership or aggregation.

### The middleware barrel is empty

[`backend/src/middlewares/index.js`](../../backend/src/middlewares/index.js) is empty. Middleware consumers currently import concrete files directly. The target conventions do not establish whether a middleware barrel is required.

### Controller error formatting overlaps centralized error handling

[`backend/src/controllers/file.controller.js`](../../backend/src/controllers/file.controller.js) directly constructs error responses for a missing upload, a missing public ID, and a Cloudinary “not found” result. [`backend/src/middlewares/error-handler.js`](../../backend/src/middlewares/error-handler.js) separately owns the centralized error response structure for forwarded errors. The current code therefore has overlapping ownership of expected error formatting.

Controllers are still responsible for translating service outcomes into HTTP semantics under the target convention. The precise boundary between controller translation and centralized response formatting remains a human decision.

### Required configuration values also have unreachable defaults

[`backend/src/config/index.js`](../../backend/src/config/index.js) first rejects absent values in its required-variable loop, then supplies defaults for several of those same values. Examples include `NODE_ENV`, `PORT`, `MONGODB_SERVER_SELECTION_TIMEOUT_MS`, `JWT_EXPIRES_IN`, `JWT_ALGORITHM`, `BCRYPT_SALT_ROUNDS`, `SMTP_PORT`, and `MAIL_FROM_NAME`. When these variables are absent, validation throws before the defaults can be used.

### Input and business validation ownership overlaps

The file controller checks that a delete `publicId` is a non-empty string, and [`backend/src/services/file.service.js`](../../backend/src/services/file.service.js) repeats the same check. Upload existence is checked in the controller, while content type is checked in middleware. The repository does not yet apply a uniform distinction among transport validation, normalization, and business validation.

### Placeholder barrels and database seed files have no active responsibility

The middleware barrel and model barrel do not aggregate their layers. [`backend/src/database/seed.js`](../../backend/src/database/seed.js) is empty, and `backend/src/database/seeds/seed-1.js` contains only a placeholder comment. These files currently represent structure rather than active behavior.

## Decisions still requiring human confirmation

The target conventions do not settle these points:

- whether model and middleware barrel files are required public entry points, optional conveniences, or unwanted;
- the exact boundary between controller-owned HTTP error translation and centralized error-body formatting;
- which configuration values are truly required and which are intended to have defaults;
- whether request-shape validation belongs in route middleware, controllers, or a consistent combination of both; and
- whether seed scaffolding is intended to remain and, if so, what module owns seed orchestration.

Until those decisions are made, code must not establish a second owner or infer a new architectural layer.
