# Backend Sources of Truth

## Purpose

This document identifies the single canonical owner for each backend responsibility. It is normative for new and changed code, while the **Current known mismatches** section records where the repository does not yet match the target convention.

“Allowed consumers” identifies which code may import or call the owner. It does not grant a consumer permission to take ownership of the responsibility itself.

## Ownership table

| Responsibility | Canonical owner | Allowed consumers | Forbidden duplication |
| --- | --- | --- | --- |
| Process entry point, server startup, shutdown, and process-level failure handling | `backend/index.js` | Package start scripts and the Node.js runtime | Additional server listeners, startup entry points, or process signal owners elsewhere |
| Express application instance and composition order | `backend/src/app.js` | `backend/index.js`; application-level test harnesses when introduced | Creating another production Express app or composing global application middleware in feature modules |
| Dotenv loading | `backend/src/config/index.js` | No other module loads dotenv | Loading dotenv in any other module or establishing multiple dotenv loaders |
| Reading environment variables | `backend/src/config/index.js` | No other module reads `process.env`; consumers use its exported configuration | Any direct `process.env` access in another module, including another module inside `src/config/` |
| Environment validation, parsing, normalization, and approved defaults | `backend/src/config/index.js` | Any backend module may consume the normalized exported values where appropriate | Secondary environment parsers, validators, normalizers, defaults, or config objects representing the same values |
| MongoDB/Mongoose connection lifecycle | `backend/src/config/mongodb.js` | Startup/shutdown owner; approved database tooling | Calling `mongoose.connect()` or owning connection lifecycle in routes, controllers, services, models, or new connection modules |
| One-time database migration invocation and connection orchestration | `backend/scripts/run-migration.js` | Explicit package scripts and operators executing an approved migration | Running migrations from application startup, request paths, seed files, or a second migration runner |
| Versioned migration data transformations | `backend/src/database/migrations/<versioned-name>.js` | Canonical migration runner and focused migration tests | Embedding migration transformations in services, models, startup, seeds, or parallel migration locations |
| Platform ExperienceLevel codes (V4 F06) | `backend/src/constants/experience-level.js` | ExperienceLevel persistence and dataset initialization | Inline redefinition of ExperienceLevel codes or expanding beyond the locked six-member set |
| ExperienceLevel persistence model | `backend/src/models/experience-level.model.js` | Approved migration tooling and future read consumers | A second ExperienceLevel schema/model, extra catalog metadata fields, or runtime CRUD/service ownership |
| ExperienceLevel canonical dataset initialization (V4 F06) | `backend/src/database/migrations/v4-experience-level-dataset.js` | Canonical migration runner and focused migration tests | Initializing ExperienceLevel from seeds, startup, request paths, or a second migration/seed owner |
| Cloudinary configured client and connection verification | `backend/src/config/cloudinary.js` | Startup owner and services that perform Cloudinary operations | Reconfiguring Cloudinary or creating another configured Cloudinary client elsewhere |
| SMTP/Nodemailer transport | `backend/src/config/mailer.js` | Mail service | Creating parallel SMTP transporters or reading SMTP environment variables directly instead of consuming `src/config/index.js` |
| Sending application email | `backend/src/services/mail.service.js` | Controllers and other services that coordinate an approved workflow | Direct `transporter.sendMail()` calls outside the mail service or additional general-purpose mail senders |
| Root API router | `backend/src/routes/index.js` | Express application | Additional root API router registries or mounting feature routers directly in the entry point |
| Feature endpoint declarations and middleware chains | `backend/src/routes/<name>.routes.js` | Root router | Defining the same feature endpoint in multiple routers; embedding business logic or database access in routes |
| HTTP request/response translation for a feature | `backend/src/controllers/<name>.controller.js` | Feature routes | HTTP handling in services/models/utils; database/model access or business workflow ownership in controllers |
| Feature business workflows and business validation | `backend/src/services/<name>.service.js` | Controllers and other approved services | Reimplementing the workflow in routes, middleware, controllers, utils, or parallel services |
| Mongoose schema, model, and persistence representation | `backend/src/models/<name>.model.js` | Services and approved database tooling | Duplicate schemas/models or model ownership in controllers, routes, middleware, or utils |
| V13 Notification type vocabulary | `backend/src/constants/notification-type.js` | Notification persistence, Notification workflows, and approved source services that declare a canonical event type | Inline Notification type strings or a second Notification type owner |
| V13 Notification persistence | `backend/src/models/notification.model.js` | `backend/src/services/notification.service.js` and approved database tooling | A second Notification schema/model, direct Notification persistence from source services or the recovery worker, or a legacy V12 Notification representation |
| V13 NotificationEvent persistence | `backend/src/models/notification-event.model.js` | `backend/src/services/notification.service.js` and approved database tooling | A second event/outbox schema, optional `eventId` Notification ownership, or a parallel generic domain-event log |
| V13 durable Notification obligation, recipient snapshot materialization, and pending-event recovery | `backend/src/services/notification.service.js` | Canonical source services inside their existing transaction boundaries; Notification read workflows; `backend/src/workers/notification-recovery.worker.js` | Moving ownership of a source transition into the Notification service; recomputing recipient snapshots during recovery; emitting realtime delivery from Slice 01; direct Notification writes in source services or the worker |
| V13 Notification recovery scheduling and process lifecycle | `backend/src/workers/notification-recovery.worker.js` (bounded non-overlapping scheduling); `backend/index.js` (explicit start after database/index readiness and stop before MongoDB disconnect) | Process entry point and focused worker/service tests | Starting work on module import; a second scheduler; direct model access from the worker; unbounded scans; treating process-local non-overlap as exactly-once delivery |
| V13 Direct Application Notification (Slice 03; F02; BR-09–BR-12, BR-47–BR-49; TX-01) | `backend/src/services/application.service.js` (`directApplyToJob` — Application create + required `DIRECT_APPLICATION_CREATED` `NotificationEvent` in one MongoDB session; Candidate self-notification exception + current Job Primary recipient/content snapshots from trusted Job/CompanyMember state; recipient User dedupe; best-effort post-commit `materializeNotificationEvent`); durable obligation/materialization/recovery remain in `backend/src/services/notification.service.js` | Existing Candidate Direct Apply route after canonical access authentication and Candidate authorization (`POST /api/candidate/applications`) | Auto-Assigning Application to Primary; moving Direct Apply ownership into the Notification service; client-supplied Candidate/Primary/Assignment recipients; writing inbox `Notification` documents from Apply outside the Notification materialization helper; Assignment, Chat, Interview, Availability, realtime, or other source-workflow Notification integration from this slice |
| V13 Assignment Notification (Slice 05; F03; BR-10–BR-19, BR-47–BR-50; TX-01) | `backend/src/services/application.service.js` (`firstAssignApplication`, `executePrimaryCurrentAssigneeMutation`, `automaticallyUnassignApplication`, and `createAssignmentNotificationEvent` — winning Assign/Reassign/Unassign CAS plus exactly one required assignment-type `NotificationEvent` in the source transaction; trusted candidate/outgoing/new-assignee snapshots; human actor filtering; generic candidate Unassign content; independent V11 SYSTEM Message `CHAT_MESSAGE_CREATED` obligations); durable obligation/materialization/recovery remain in `backend/src/services/notification.service.js` | Existing authorized Assignment routes and trusted V10 automatic-Unassign lifecycle/team callers | Moving Assignment authority or source transitions into the Notification service; client-supplied actor/recipient/assignee snapshots; Reassign as Unassign + Assign; inbox writes from Assignment sources; actor inference for automatic Unassign; lifecycle-cause disclosure; Pipeline, Interview, or realtime behavior |
| V13 Pipeline + Withdraw + Availability Request Notification (Slice 06; F04, F05 partial; BR-10, BR-11, BR-20–BR-24, BR-47–BR-50; TX-01) | `backend/src/services/application.service.js` (`updateApplicationRecruitmentPipelineStatus`, `createInterviewProposal` first-proposal cutover, `withdrawApplication`, and `createApplicationLifecycleNotificationEvent` — winning source CAS plus required lifecycle `NotificationEvent` obligations in the same transaction; Candidate recipient for status/hired/rejected/availability-request; Withdraw snapshots current Assignee or Job Primary when UNASSIGNED with Candidate actor filtering; independent dual events on `→ CONTACTED`; best-effort post-commit materialization); durable obligation/materialization/recovery remain in `backend/src/services/notification.service.js` | Existing Pipeline, first Interview Proposal, and Candidate Withdraw routes/workflows | Changing V10/V12 source lifecycles or concurrency; Availability first-submit / Schedule / Assignment Notification redesign; client-supplied Candidate/Assignee/Primary recipients; inbox writes outside Notification materialization; expanding Direct Apply self-notification into Pipeline; realtime |
| V13 Candidate Availability First Submit Notification (Slice 07; F05 closure; BR-25–BR-27, BR-47–BR-49; TX-01) | `backend/src/services/application.service.js` (`submitCandidateAvailabilityFirstTime` — serializes the winning Application assignment snapshot without changing Application business state, then persists the sole current `CandidateAvailability` plus required `INTERVIEW_AVAILABILITY_SUBMITTED` `NotificationEvent` in one transaction when ASSIGNED; trusted current CompanyMember→User recipient and Job-title content snapshot; UNASSIGNED creates no event/fallback; best-effort post-commit materialization); durable obligation/materialization/recovery remain in `backend/src/services/notification.service.js` | Existing Candidate first-submit Availability route after canonical access/Candidate authorization (`POST /api/candidate/applications/:applicationId/availability`) | Availability edit Notification; Primary/outgoing/future-Assignee fallback; changing V12 current-set/revision/proposal-lock lifecycle; Interview Schedule Notification; direct inbox writes outside Notification materialization; realtime |
| Cross-cutting request processing | `backend/src/middlewares/<name>.js` | Express application and feature routes | Business workflows in middleware or duplicate middleware for an already-owned concern |
| Session-bound access authentication | `backend/src/services/authenticate-access.service.js` (validation); `backend/src/middlewares/authenticate-access.js` (HTTP extraction and request enrichment) | Protected feature routes and their middleware chains | Reimplementing access/session validity checks in controllers, routes, utils, or parallel auth middlewares |
| Company Manager onboarding access authentication | `backend/src/services/authenticate-access.service.js` (`authenticateOnboardingAccess`); `backend/src/middlewares/authenticate-onboarding-access.js` | V2 Company onboarding routes that require Company Staff Manager membership semantics with `PENDING_ACTIVATION` | Treating onboarding access as ACTIVE-account access or reimplementing onboarding auth outside the canonical owners |
| Company self-service GET/PATCH access composition | `backend/src/middlewares/authenticate-company-self-service-access.js` | Company GET/PATCH `/` routes | Parallel dual-mode auth for company self-service, or folding ACTIVE F09 access into onboarding auth |
| Session access for logout (ACTIVE or onboarding CM) | `backend/src/services/authenticate-access.service.js` (`authenticateSessionAccess`); `backend/src/middlewares/authenticate-session-access.js` | Logout route | Parallel logout credential checks outside the session-access owners |
| Platform Admin authorization | `backend/src/middlewares/authorize-platform-admin.js` | Platform-admin routes after access authentication | Role checks for Platform Admin workflows duplicated in controllers, routes, or services |
| Category level enum (`FIELD`, `POSITION`) | `backend/src/constants/category-level.js` | Category persistence and workflows | Inline redefinition of Category levels or a third level value |
| Category persistence model | `backend/src/models/category.model.js` | Category service and approved database tooling | A second `Category` schema/model, client-authored `normalizedName`, or direct use from routes/controllers/middleware |
| Platform Admin create Category FIELD (V4 F01) | `backend/src/services/category.service.js` (`createFieldCategory`) | Category / platform-admin controllers | Parallel FIELD-create workflows outside the category service; duplicating Platform Admin role checks in the service; accepting client `level`/`parentCategoryId` for FIELD creation |
| Platform Admin create Category POSITION (V4 F02) | `backend/src/services/category.service.js` (`createPositionCategory`) | Category / platform-admin controllers | Parallel POSITION-create workflows outside the category service; duplicating Platform Admin role checks in the service; allowing missing/non-FIELD parents; mutating the parent FIELD |
| Platform Admin account lock (V1 F10; V3 User↔CompanyMember independence; V10 F11 / Slice 09 / Slice 10 TX-05) | `backend/src/services/platform-admin.service.js` (`lockAccount`); Application detach via Slice 06 `automaticallyUnassignCurrentResponsibilitiesOfRecruiter` after User lifecycle commit; already-`LOCKED` retry reconciles remaining Application responsibilities without changing business intent to TERMINATED | Platform-admin controllers | Parallel account-lock workflows in auth or other services; requiring Recruiter Job/Application zero-responsibility, replacement selection, Application `A → B` handoff, or Job-team mutation before generic User `ACTIVE → LOCKED`; mutating `Application.status` / `submittedCvSnapshot` / Recruitment Team / `CompanyMember.status` from Platform Admin lock; treating system automatic Unassign as Platform Admin assignment authority; granting Platform Admin First Assign / Reassign / Take over / administrative handoff / Pipeline; forcing TERMINATE solely to finish partial Unassign after LOCK |
| Platform Admin account terminate (V1 F11; V3 User↔CompanyMember independence; V10 F11 / Slice 09 / Slice 10 TX-05) | `backend/src/services/platform-admin.service.js` (`terminateAccount`); Application detach via Slice 06 `automaticallyUnassignCurrentResponsibilitiesOfRecruiter` after User lifecycle commit; already-`TERMINATED` retry reconciles remaining Application responsibilities without a new lifecycle transition | Platform-admin controllers | Parallel account-termination workflows in auth or other services; requiring Recruiter Job/Application zero-responsibility, replacement selection, Application `A → B` handoff, or Job-team mutation before canonical User terminate; mutating Application status/snapshot, Recruitment Team, or `CompanyMember.status` from Platform Admin terminate; treating system automatic Unassign as Platform Admin assignment authority; granting Platform Admin Application processing authority; treating non-TERMINATED invalid User transitions as idempotent |
| V10 Platform User eligibility loss → automatic Unassign + Company Job-team recovery boundary (Slice 09–10; F11; BR-08/BR-42/BR-46–BR-53; TX-02/TX-05) | `backend/src/services/platform-admin.service.js` (`lockAccount` / `terminateAccount`) for independent User lifecycle then Slice 06 automatic Unassign; already-`LOCKED` LOCK retry and already-`TERMINATED` TERMINATE retry reconcile remaining Applications (TX-05); `backend/src/services/application.service.js` (`automaticallyUnassignCurrentResponsibilitiesOfRecruiter` + canonical CM Assign-Reassign-Unassign) for Application detach and later Assign-again; `backend/src/services/job.service.js` (`replacePrimaryRecruiter` / `removeSupportingRecruiter`, unfinished recovery gates, shared H3 eligibility acquires) for separate Job-team recovery | Existing Platform Admin account routes; existing CM assign / reassign / unassign / force-reassign, replace-Primary, and remove-Supporting routes | Applying CompanyMember final-zero guard to generic User lifecycle; requiring Application replacement or synthesizing `A → B` from Platform Admin lock/terminate; mutating Job Primary/Supporting from Platform Admin account action; synchronizing `User.status` with `CompanyMember.status`; trusting client-declared company/outgoing identity as authority; Company-lock Application Unassign; automatic/random replacement; recovery persistence/history/queue/worker |
| Platform Admin company registration review (V2 F04) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-registration list/detail workflows outside the platform-admin service |
| Platform Admin reject Company (V2 F06) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-reject workflows outside the platform-admin service |
| Platform Admin approve Company / TX-02 confirmation issue (V2 F05) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Platform Admin company-approve or confirmation-issue workflows outside the platform-admin service |
| Platform Admin lock Company / TX-04 (V2 F10) | `backend/src/services/platform-admin.service.js` | Platform-admin controllers | Parallel Company lock or CM termination-on-lock workflows outside the platform-admin service |
| Unmatched routes | `backend/src/middlewares/not-found.js` | Express application | Feature-specific or application-wide replacement 404 handlers elsewhere |
| Final centralized HTTP error handling | `backend/src/middlewares/error-handler.js` | Express application; errors forwarded by the request pipeline | Competing global error handlers or independently defined global error response contracts |
| Operational application error type | `backend/src/utils/app-error.js` | Routes, middleware, controllers, services, and other helpers as appropriate | Additional general-purpose application error classes representing the same contract |
| Generic reusable helpers | The relevant module under `backend/src/utils/` | Any layer for which the helper is dependency-safe | Feature business workflows, HTTP handling, or persistence ownership hidden in utils |
| Shared enum-like constants and fixed mappings | The relevant module under `backend/src/constants/` | Any backend layer that needs the value | Multiple constant modules defining the same domain concept or inline redefinition of canonical values |
| Platform Location vocabulary (V4 F03) | `backend/src/constants/location.js` | Any backend layer that needs Location values | A second Location member list, persisted Location catalog, Remote-as-Location member, or auto-synced/renamed administrative geography set |
| Platform EmploymentType vocabulary (V4 F04) | `backend/src/constants/employment-type.js` | Any backend layer that needs EmploymentType values | A second EmploymentType member list or persisted EmploymentType catalog |
| Platform WorkMode vocabulary (V4 F05) | `backend/src/constants/work-mode.js` | Any backend layer that needs WorkMode values | A second WorkMode member list, persisted WorkMode catalog, or treating `REMOTE` as a Location |
| Platform account types (`CANDIDATE`, `COMPANY_STAFF`, `PLATFORM_ADMIN`) | `backend/src/constants/user-role.js` | Any backend layer that needs platform roles | Inline redefinition of platform account types or restoring removed `COMPANY_MANAGER` on `User.role` |
| Company membership roles (`COMPANY_MANAGER`, `RECRUITER`) | `backend/src/constants/company-member-role.js` | Company membership persistence and workflows | Defining Company roles on `User.role` or a second Company-role owner |
| Company membership statuses (`ACTIVE`, `LOCKED`, `TERMINATED`) | `backend/src/constants/company-member-status.js` | Company membership persistence and workflows | A second Company-membership status owner |
| Job lifecycle statuses (`DRAFT`, `PENDING_APPROVAL`, `PUBLISHED`, `CLOSED`, `EXPIRED`) | `backend/src/constants/job-status.js` | Job persistence and workflows | Inline redefinition of Job statuses or inventing Product-excluded states such as `REJECTED` |
| V1/V2/V3 authentication-token types (`EMAIL_VERIFICATION`, `PASSWORD_RESET`, `COMPANY_APPROVAL_CONFIRMATION`, `RECRUITER_ACTIVATION`) | `backend/src/constants/auth-token-type.js` | Authentication-token persistence and workflows | Additional authentication-token type owners or unsupported authentication-token types |
| File storage operations | `backend/src/services/file.service.js` (`uploadFileBuffer`, `deleteFile`, `downloadFileBuffer`) | Controllers and approved services | Direct Cloudinary upload/delete/download operations in routes, controllers, middleware, models, or another general file service; treating temporary signed delivery URLs as public V7 CandidateCV access semantics or persisted canonical state; making restricted delivery the global default for every upload instead of an explicit caller option |
| User persistence model | `backend/src/models/user.model.js` | Services and approved database tooling | A second `User` schema/model or direct use from routes/controllers/middleware |
| Candidate self Profile read/update (V7 F01) | `backend/src/models/user.model.js` (existing persistence); `backend/src/services/candidate-profile.service.js` (Profile workflow and allowed-field enforcement) | Candidate Profile controllers after canonical access authentication | A separate CandidateProfile collection/model; Profile persistence outside User; direct User-model access from routes/controllers/middleware; allowing F01 to mutate email, role, status, password/authentication fields, sessions, or tokens |
| CandidateCV source types (`GENERATED`, `UPLOADED`) | `backend/src/constants/candidate-cv-source-type.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV source types or separate Generated/Uploaded root collections |
| CandidateCV statuses (`DRAFT`, `ACTIVE`) | `backend/src/constants/candidate-cv-status.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV statuses or inventing Product-excluded states such as `ARCHIVED` as `status` |
| CandidateCV visibility (`PRIVATE`, `PUBLIC`) | `backend/src/constants/candidate-cv-visibility.js` | CandidateCV persistence and workflows | Inline redefinition of CandidateCV visibility or treating `PUBLIC` as effective anonymous/public access |
| Generated CV language proficiency (`BEGINNER`, `INTERMEDIATE`, `ADVANCED`, `FLUENT`, `NATIVE`) | `backend/src/constants/cv-language-proficiency.js` | CandidateCV Generated content persistence | A second proficiency vocabulary or free-form proficiency values outside the canonical enum |
| Application source (`DIRECT_APPLICATION`) (V9 S01) | `backend/src/constants/application-source.js` | Application persistence and approved Application workflows | Inline redefinition of Application sources or predeclaring `RECRUITER_INVITATION`, Source Recruiter, or Source Invitation before their owning version |
| Application statuses (V9: `APPLIED`, `WITHDRAWN`; V10 extends the approved vocabulary) | `backend/src/constants/application-status.js` | Application persistence and approved Application workflows | Inline redefinition of Application statuses or predeclaring statuses beyond their approved owning version |
| Direct Apply / PT-01 / TX-01 (V9 S02–S03 F01–F03) | `backend/src/services/application.service.js` (`directApplyToJob`, Generated/Uploaded snapshot capture helpers) | Candidate application controller after canonical access authentication and Candidate authorization | Parallel Apply workflows outside the application service; client-supplied Candidate identity; temporary application-only CV or combined upload+apply endpoint; Default CV auto-apply; Job/Company/CandidateCV/Recruitment Team mutation on Apply; duplicate `companyId` or recruiter assignment fields; inventing Replace/Withdraw/My Applications from Apply |
| Application persistence model, current Submitted CV Snapshot representation, local state matrices, revision, Candidate–Job uniqueness, and V10 current Assignee representation | `backend/src/models/application.model.js` | Application services and approved database tooling | A second Application schema/model; separate snapshot/history collections; duplicate `companyId`; `sourceRecruiterCompanyMemberId`, Invitation fields, assignment/status history, Job snapshot, Application-based Job deletion state or guard, or direct model use from routes/controllers/middleware |
| V10 `ASSIGN / UNASSIGN` Application persistence foundation (current Slice 01; F01–F04/F11 persistence enabler; BR-03–BR-05/BR-10/BR-17/BR-20/BR-28/BR-52; PI-07/PI-10/PI-14/PI-23/PI-26) | `backend/src/constants/application-status.js`; `backend/src/models/application.model.js` (nullable `assignedRecruiterCompanyMemberId`, local and collection status × assignment-state matrices, revision/CAS metadata, indexes and existing invariants) | Approved Application workflows and database tooling | Creating a parallel Application model/constants owner; adding `UNASSIGNED` as an Application status; requiring a current Assignee for `SCREENING`, `CONTACTED`, `INTERVIEW_SCHEDULED`, or `INTERVIEW_COMPLETED`; allowing `HIRED` or `REJECTED` without the final Assignee; adding `sourceRecruiterCompanyMemberId`, Invitation source, history/KPI/workload persistence, Job snapshot, new assignment fields/indexes/collections/backfill, or an Application↔Job delete transaction not required by V10 |
| V10 V9 compatibility + Job-retention compatibility (S02; F01, F09) | Existing V9 Application workflows in `backend/src/services/application.service.js` (`directApplyToJob`, `replaceSubmittedCv`, `withdrawApplication`); V5 Job lifecycle retention in `backend/src/services/job.service.js` (`closePublishedJob`, `expirePublishedJobIfDue`, `deletePrePublicationJob` / pre-publication delete authority) | Candidate application controllers and Job lifecycle controllers | Reimplementing Apply/Replace/Withdraw outside the application service; inventing Application-existence Job delete guards or Apply↔Delete transactions; cascading Application deletes or nulling `Application.jobId`; auto-mutating Application when Job becomes `CLOSED`/`EXPIRED`; allowing Candidate Replace/Withdraw outside exact `APPLIED`; clearing Assignee on Withdraw; adding Assign/Pipeline/My Applications/projections from compatibility work |
| V10 Unassigned Applications + Primary Application View (S03; F01) | `backend/src/services/application.service.js` (`listPrimaryJobApplications`, Unassigned derivation via absent/null `assignedRecruiterCompanyMemberId`; assignment-management read for current Primary or owning-Company Manager) | Job routes after canonical access authentication and Company Staff authorization (`GET /api/jobs/:jobId/applications`) | Parallel Application list/read outside the application service; treating Unassigned as an `Application.status`; granting Supporting/Platform Admin/Candidate this view; expanding Application read into CandidateCV My CVs library access or snapshot-delivery authority; mutating status/assignee/snapshot/identity on read; inventing Assign/Reassign/Take over, Pipeline, Managed Jobs aggregates, workload counters, or My Applications from this slice |
| V10 Assign Unassigned Application (Slice 02; F02, F09; BR-06–BR-11/BR-17/BR-27/BR-36–BR-38/BR-40; TX-01/TX-02) | `backend/src/services/application.service.js` (`firstAssignApplication` via `commitAssignFromUnassigned`; NONE→Recruiter CAS via `version` + null/missing assignee + current non-terminal status; TX-02 Assignee eligibility at commit via shared Company + CompanyMember + User + Job-team acquires; manual actor business-access + Primary relation at commit via `assertAssignmentManagementActorBusinessAccessAtCommit` then target eligibility then `assertAssignmentManagementPrimaryRelationAtCommit`; no Job-status gate for CLOSED/EXPIRED; actor is current Primary or owning-Company Manager) | Job routes after canonical access authentication and Company Staff authorization (`POST /api/jobs/:jobId/applications/:applicationId/assign`) | Parallel Assign outside the application service; Supporting self-claim; trusting client company/team/authority fields; mutating status/snapshot/identity on Assign; requiring `APPLIED` only; Unassign / Assignment History / Reassign / Take over / automatic Unassign / Pipeline / My Applications from this slice |
| V10 Primary Reassign / Take over / Unassign Application (current Slice 03; F03, F09, F10; BR-10–BR-14/BR-17–BR-19/BR-27/BR-33–BR-38; TX-01/TX-02/TX-03) | `backend/src/services/application.service.js` (`reassignApplication` / `unassignApplication` via `executePrimaryCurrentAssigneeMutation` + `commitAssignedAssigneeMutation`; Assigned A→B or A→NONE CAS via `version` + expected assignee + non-terminal status; Take over = Reassign onto current Primary; TX-02 Assignee eligibility at commit only when target ≠ NONE; manual actor business-access at commit for all manual A→B/A→NONE including Unassign via `assertAssignmentManagementActorBusinessAccessAtCommit` + `assertAssignmentManagementPrimaryRelationAtCommit`; no Job-status gate for CLOSED/EXPIRED; actor is current Primary or owning-Company Manager) | Job routes after canonical access authentication and Company Staff authorization (`POST /api/jobs/:jobId/applications/:applicationId/reassign`, `POST /api/jobs/:jobId/applications/:applicationId/unassign`) | Parallel Reassign/Take over/Unassign outside the application service; Supporting self-reassign/self-takeover/self-unassign/claim; turning `A→B` into `A→NONE→B`; mutating status/snapshot/identity/Recruitment Team on assignment mutation; persisting takeover flags/history; automatic eligibility-loss Unassign; bulk assignment; new fields/collections/indexes/workload counters; Pipeline authority from Unassign; committing manual Unassign on stale Primary, non-operational Company, or actor User/CompanyMember business-access loss |
| V10 Company Manager Application Assignment Management (current Slice 04; F01, F02, F04, F09; BR-06–BR-08/BR-10–BR-11/BR-15–BR-17/BR-27/BR-36–BR-38/BR-40/BR-42/BR-53; TX-01/TX-02/TX-03) | `backend/src/services/application.service.js` (`assertAssignmentManagementAuthority` + `assertAssignmentManagementActorBusinessAccessAtCommit`; CM uses the same `firstAssignApplication` / `reassignApplication` / `unassignApplication` / `commitAssignFromUnassigned` / `commitAssignedAssigneeMutation` / TX-02 eligibility-at-commit owners as Primary; tenant from authenticated CM membership → owning Company → Job → Application; Company operational + CM Membership/User business-access acquire at manual commit including Unassign) | Job routes after canonical access authentication and Company Staff authorization (`GET /api/jobs/:jobId/applications`, `POST .../assign`, `POST .../reassign`, `POST .../unassign`); CM-only compatibility `POST .../force-reassign` | A CM-specific assignment engine; recovery-only CM assignment restriction; making CM the Assignee; granting CM Pipeline / snapshot-delivery / CandidateCV library access; trusting client-supplied company identity; mutating status/snapshot/identity/Recruitment Team; automatic Unassign; CompanyMember LOCK/TERMINATE or team-removal changes; Platform Admin assignment authority; history/queue/worker/field/collection/index/migration/workload counters |
| V10 Canonical Automatic-Unassign Primitive (current Slice 06; F04, F09, F11; BR-08/BR-10–BR-11/BR-17/BR-28/BR-31/BR-33–BR-38/BR-48–BR-53; TX-01/TX-02/TX-05) | `backend/src/services/application.service.js` (`automaticallyUnassignApplication` via shared `commitAssignedAssigneeMutation`; `automaticallyUnassignCurrentResponsibilitiesOfRecruiter` / `automaticallyUnassignCurrentResponsibilitiesOfRecruiterOnJob` independently loop current non-terminal assignments with no global transaction and no persisted progress/recovery state; A→NONE CAS via `version` + expected outgoing Assignee + non-terminal status; no replacement / no synthetic A→B; terminal Applications keep final Assignee; V11 F05 adds per-Application Conversation SYSTEM Message under TX-04 when Conversation exists) | Trusted internal lifecycle/team orchestration callers (not a public HTTP surface); CompanyMember LOCK/TERMINATE (Slice 07), Recruitment Team removal (Slice 08), and Platform Admin User lifecycle (Slice 09) consume this primitive | A second assignment mutation engine; public automatic-Unassign HTTP; requiring replacement or synthesizing A→B; mutating status/snapshot/identity/Recruitment Team; wiring this primitive into Platform User lifecycle before that slice; global all-or-nothing multi-Application transaction; progress/recovery/queue/worker/history persistence; field/collection/index/migration/workload counters |
| V10 Administrative Forced Reassignment compatibility (historical S06; now Slice 04 CM A→B wrapper) | `backend/src/services/application.service.js` (`forceReassignApplication` as CM-only compatibility A→B over `executePrimaryCurrentAssigneeMutation`; no recovery-only restriction; shared TX-02 target eligibility + shared manual actor-authority commit boundary) | Job routes after canonical access authentication and Company Manager authorization (`POST /api/jobs/:jobId/applications/:applicationId/force-reassign`) | Parallel forced reassignment outside the application service; making CM the Assignee; granting CM Pipeline; persisting forced flags/history/audit; reintroducing trusted pre-lifecycle A→B handoff; automatic Unassign / LOCK/TERMINATE/team-removal orchestration from this surface |
| V10 CompanyMember Recruiter LOCK/TERMINATE automatic Unassign integration (current Slice 07; F04, F09, F11; BR-08/BR-27–BR-28/BR-36–BR-38/BR-46/BR-50–BR-53; TX-02/TX-05) | `backend/src/services/recruiter.service.js` (`lockRecruiter` / `terminateRecruiter` via shared `executeUnifiedResponsibilityHandoffBeforeLifecycleCompletion`); Application detach via Slice 06 `automaticallyUnassignCurrentResponsibilitiesOfRecruiter`; Job-team via V6 forced Primary transfer + Supporting removal; dual final zero-responsibility guard from current persisted state | Existing Company Recruiter lock/terminate routes after CM authorization (`transfers[]` remains Job Primary replacement context only) | Requiring Application replacement or synthesizing A→B Application handoff in CompanyMember lifecycle; creating `NONE Primary`; redesigning Job-team model; inventing parallel team-removal / Platform User Application orchestration; Company-lock Application mutation; Pipeline / Managed Jobs / My Applications / workload counters; Assignment/Status/lifecycle history; persisted responsibility counters; global all-or-nothing Recruiter transaction |
| V10 Recruitment Team removal automatic Unassign integration (current Slice 08; F09, F11; BR-08/BR-27–BR-28/BR-36–BR-38/BR-50–BR-52; TX-02/TX-05) | `backend/src/services/job.service.js` (`removeSupportingRecruiter` / `replacePrimaryRecruiter`); Job-scoped Application detach via `application.service.js` (`automaticallyUnassignRecruiterApplicationsOnJobForTeamRemoval` → Slice 06 `automaticallyUnassignCurrentResponsibilitiesOfRecruiterOnJob` + Job-scoped outstanding Application guard); V6 Primary replacement / Supporting membership gates unchanged | Existing V6 team routes after CM/Primary authorization; F11 unfinished-Job recovery is CM-only and requires persisted outgoing Platform User ineligibility | Synthesizing Application A→B / requiring Application replacement before team removal; inventing Application replacement heuristics; Unassign solely because Primary↔Supporting role changes while Recruiter remains on-team and eligible; creating `NONE Primary`; expanding normal team mutation to CLOSED/EXPIRED; Company-lock Application reassign/unassign; CompanyMember LOCK/TERMINATE redesign; Platform Admin User lifecycle Application orchestration from this slice; Pipeline / workload/history persistence |
| V10 Platform Admin Recruiter User LOCK/TERMINATE automatic Unassign integration (current Slice 09; F11; BR-08/BR-42/BR-46–BR-53; TX-02/TX-05) | `backend/src/services/platform-admin.service.js` (`lockAccount` / `terminateAccount`); User lifecycle commits first per V1 (status + session revoke) without Job/Application zero-responsibility guard; Application detach via Slice 06 `automaticallyUnassignCurrentResponsibilitiesOfRecruiter` from persisted Recruiter CompanyMember; repeated LOCK of already-`LOCKED` and repeated TERMINATE of already-`TERMINATED` are the TX-05 same-intent reconciliation paths for remaining responsibilities; Job Primary/Supporting and `CompanyMember.status` unchanged | Existing Platform Admin account routes after Platform Admin authorization | Applying CompanyMember final-zero guard to generic User lifecycle; requiring Application replacement or synthesizing A→B; mutating Job-team / CompanyMember / Application status/snapshot from Platform Admin account action; Company-lock Application Unassign; Platform Admin assignment/pipeline authority; progress/recovery/queue/worker/history persistence; forcing TERMINATE solely to finish partial Unassign after LOCK |
| V10 Recruitment Pipeline (S10; F05, F09 partial) | `backend/src/services/application.service.js` (`updateApplicationRecruitmentPipelineStatus`; current-Assignee-only status CAS via expected status + version + assignee; continuous eligibility + shared TX-02 multi-dimension acquires at commit; status/version only) | Job routes after canonical access authentication and Recruiter authorization (`POST /api/jobs/:jobId/applications/:applicationId/pipeline`) | Parallel Pipeline outside the application service; granting Primary/CM/Platform Admin/Candidate pipeline authority without current Assignee responsibility; skip/backward/reopen/`WITHDRAWN` Recruiter transitions; mutating assignee/snapshot/identity; Interview Schedule entity / status history / timestamps; Job accepting/public eligibility gate for existing Applications; auto Reassign/Unassign/team mutation from Pipeline; Managed Jobs / My Applications / workload counters from this slice |
| V10 Managed Jobs + Pipeline Workspace + Current Workload (historical S11; current Slice 05 read compatibility; F06, F10) | `backend/src/services/application.service.js` (`listManagedJobs`, `getManagedJobPipelineWorkspace`; derived Unassigned/pipeline counts/workload from Job Primary + Application state; Unassigned at any non-terminal status stays in its Recruitment Status group and the independent Unassigned filter; Assignee/Unassigned filters use current `assignedRecruiterCompanyMemberId`; no persisted ManagedJob/Kanban/workload counters) | Job routes after canonical access authentication and Recruiter authorization (`GET /api/jobs/managed`, `GET /api/jobs/:jobId/workspace`) | Parallel Managed Jobs outside the application service; treating Unassigned as status or excluding Unassigned Applications from their pipeline group; company-global workload beyond actor Managed Jobs; Supporting/CM/Candidate/Platform Admin Primary workspace authority; My Applications; persisted KPI/history/Kanban/ManagedJob collections; mutating Job/Application on read; new field/index/migration for this revision |
| V10 Recruiter My Applications (historical S12; current Slice 05 read compatibility; F07, F09 partial) | `backend/src/services/application.service.js` (`listRecruiterMyApplications`, `getRecruiterMyApplication`; current-assignee projection via `assignedRecruiterCompanyMemberId`; A→NONE/A→B remove from former Assignee; NONE→B/A→B add to new Assignee when the projection includes that status; terminals readable but excluded from active workload; no Job accepting-lifecycle filter; list membership does not grant Pipeline authority) | Job routes after canonical access authentication and Recruiter authorization (`GET /api/jobs/my-applications`, `GET /api/jobs/my-applications/:applicationId`) | Parallel My Applications outside the application service; Primary absorbing Managed-Job Applications without being Assignee; Assignment History / prior-assignee lists; CandidateCV library access from snapshot reads; Candidate My Applications; inventing Pipeline authority from list membership; persisted counters/`assignedApplicationIds[]`/search collections; mutating Application on read; new field/index/migration for this revision |
| V10 Candidate My Applications (historical S13; current Slice 05 read compatibility; F08, F09 partial) | `backend/src/services/application.service.js` (`listCandidateMyApplications`, `getCandidateMyApplication`; owner projection via authenticated `candidateUserId` at every Recruitment Status; live Assignee `fullName`/`avatarUrl`/`jobTitle` only, null/Unassigned after Unassign and the new Assignee after Assign again; Job+Company; optional status/`q` filters; no Job accepting-lifecycle filter) | Candidate routes after canonical access authentication and Candidate authorization (`GET /api/candidate/applications`, `GET /api/candidate/applications/:applicationId`) | Parallel Candidate My Applications outside the application service; trusting client `candidateUserId`; exposing Recruiter email/phone/`companyMemberId`/Assignment History; Recruiter/CM/Platform Admin Candidate My Applications authority; inventing assignee snapshots or CandidateApplication collections; mutating Application/Replace/Withdraw/Pipeline authority from reads; new field/index/migration for this revision |
| V10 Application submittedCvSnapshot Preview/Download (acceptance H1; F06–F08; BR-31) | `backend/src/services/application.service.js` (`previewCandidateApplicationSubmittedCv`, `downloadCandidateApplicationSubmittedCv`, `previewPrimaryJobApplicationSubmittedCv`, `downloadPrimaryJobApplicationSubmittedCv`, `previewRecruiterMyApplicationSubmittedCv`, `downloadRecruiterMyApplicationSubmittedCv`; delivers persisted `submittedCvSnapshot.pdfFile` via `file.service.js` + Application submitted-CV storage policy) | Candidate routes (`GET /api/candidate/applications/:applicationId/submitted-cv/preview\|download`); Job Primary routes (`GET /api/jobs/:jobId/applications/:applicationId/submitted-cv/preview\|download`); Recruiter My Applications routes (`GET /api/jobs/my-applications/:applicationId/submitted-cv/preview\|download`) after canonical access authentication and Candidate/Recruiter authorization | Parallel snapshot file delivery outside the application service; falling back to live CandidateCV / `sourceCandidateCvId`; exposing `pdfFile.storageKey` on Application projections; returning permanent public storage URLs; granting CM/Platform Admin/cross-tenant/non-Primary/non-Assignee snapshot access; mutating Application/snapshot/CandidateCV/Job/Assignee/`version` on read; inventing snapshot history, re-upload/copy-for-download, or CandidateCV library access from Application scope |
| V10 TX-02 Assignee eligibility coordination (acceptance H3; BR-07/BR-08/BR-28/BR-36–BR-38; TX-02) | `backend/src/services/application.service.js` (`assertAssigneeEligibleAtAssignmentCommit`); acquire helpers in `backend/src/services/job.service.js` (`acquireOperationalCompanyForAssigneeEligibilityTx`, `acquireActiveRecruiterMembershipForTeamResponsibilityTx`, `acquireActiveUserForAssigneeEligibilityTx`, `acquireJobTeamMembershipForAssigneeEligibilityTx`) | First Assign / Reassign / Take over / CM force-reassign / Recruitment Pipeline commit paths | Serializing only via CompanyMember while ignoring Company/User/Job-team eligibility loss; inventing lock collections / eligibility counters / global all-or-nothing Application transactions; mutating Application assignment/status from Company lock or generic User lifecycle solely for coordination; granting Platform Admin Assign/Reassign/Handoff authority; treating H3 User-dimension acquires as authority to add zero-responsibility gating onto V1 Platform Admin User lifecycle (that boundary is H2) |
| V10 manual assignment-management actor authority at commit (acceptance H1 actor-authority + actor lifecycle eligibility; BR-06/BR-12/BR-15/BR-53; V1/V3 Company Staff access; TX-02) | `backend/src/services/application.service.js` (`assertAssignmentManagementActorBusinessAccessAtCommit` + `assertAssignmentManagementPrimaryRelationAtCommit` on shared manual Assign/Reassign/Unassign owners); `backend/src/services/job.service.js` (`acquireOperationalCompanyForAssigneeEligibilityTx`, `acquireActiveCompanyStaffMembershipForBusinessAccessTx`, `acquireActiveUserForAssigneeEligibilityTx`, `acquireJobCurrentPrimaryForAssignmentManagementTx`) | Manual First Assign / Reassign / Take over / Unassign / CM force-reassign commit paths | Applying actor-authority acquires to automatic Unassign; inventing a parallel locking framework; Job→Membership/User lock-order inversion; committing manual Assign/Reassign/Unassign after Primary replacement, Company lock, or actor User/CompanyMember/`mustChangePassword` business-access loss with stale pre-tx context; redesigning API/fields/history/lock or recovery state |
| Platform Message types (`NORMAL`, `SYSTEM`) (V11 F01 persistence) | `backend/src/constants/message-type.js` | Message persistence and later approved Chat workflows | Inline redefinition of Message types or inventing subtypes such as `ASSIGNED`/`REASSIGNED`/`UNASSIGNED` as `Message.type` |
| Conversation persistence model (V11 Slice 01; F01; BR-01–BR-06, BR-49, BR-50, BR-53) | `backend/src/models/conversation.model.js` (`applicationId` required/immutable/unique, `createdAt` only; no Candidate/Assignee/Job/Company/participant/chat-state fields) | Application services and approved database tooling | A second Conversation schema/model; `Application.conversationId`; duplicating Candidate, current Assignee, Job, Company, participant list, or Conversation operational mode onto Conversation; a Conversation create HTTP/service outside First Assign |
| Message persistence model (V11 Slice 01; BR-13 persistence foundation) | `backend/src/models/message.model.js` (`NORMAL`/`SYSTEM`, historical `senderUserId`/`senderCompanyMemberId`, immutable content; local sender matrix) | Application services and later approved Chat workflows; approved database tooling | A second Message schema/model; rewriting historical sender identity; treating Message history as current Assignee or Chat authorization; inventing Send workflow, attachments, or edit/delete from this persistence foundation |
| V11 First Assign Conversation creation (Slice 01; F01; BR-01–BR-06, BR-49, BR-50, BR-53; TX-01) | `backend/src/services/application.service.js` (`firstAssignApplication` via existing `commitAssignFromUnassigned` then `createConversationOnFirstAssignIfAbsent` in the same transaction; no SYSTEM Message on newly created Conversation) | Existing Job Assign routes after canonical access authentication and Company Staff authorization (`POST /api/jobs/:jobId/applications/:applicationId/assign`) | A parallel Assign engine; creating Conversation before/outside successful First Assign; creating SYSTEM Message on First Assign when Conversation is newly created; mutating Recruitment Status/snapshot/identity/Recruitment Team on Conversation create; Chat send/read/authorization; duplicating V10 eligibility/CAS owners |
| V11 Reassign / Take over SYSTEM Message (Slice 02; F03; BR-15–BR-20, BR-47, BR-51; TX-02) | `backend/src/services/application.service.js` (`reassignApplication` / `forceReassignApplication` via `executePrimaryCurrentAssigneeMutation` then `createSystemMessageIfConversationExists` with responsibility-changed content after successful A→B CAS; content from `backend/src/constants/system-message-content.js`; no Conversation create) | Existing Job Reassign / force-reassign routes after canonical access authentication and Company Staff authorization (`POST /api/jobs/:jobId/applications/:applicationId/reassign`, `POST .../force-reassign`) | Creating Conversation on Reassign; Automatic Unassign SYSTEM Message; rewriting historical Message senders; treating SYSTEM Message as Assignment History or current-Assignee/authorization authority; Chat send/read; mutating Recruitment Status/snapshot/identity/Recruitment Team; duplicating V10 eligibility/CAS owners |
| V11 Manual Unassign + Assign again SYSTEM Message (Slice 03; F04, F06; BR-21–BR-25, BR-29–BR-30, BR-47, BR-51; TX-03, TX-05) | `backend/src/services/application.service.js` (`unassignApplication` via `executePrimaryCurrentAssigneeMutation` + awaiting-assignee SYSTEM Message when Conversation exists; `firstAssignApplication` Assign-again path reuses existing Conversation and writes new-assignee SYSTEM Message; First Assign with no Conversation unchanged; shared `createSystemMessageIfConversationExists` / `system-message-content.js`) | Existing Job Unassign / Assign routes after canonical access authentication and Company Staff authorization (`POST /api/jobs/:jobId/applications/:applicationId/unassign`, `POST .../assign`) | Creating a second Conversation on Assign again; SYSTEM Message on First Assign without prior Conversation; rewriting historical Message senders; treating SYSTEM Message as Assignment History/authorization; Chat send/read HTTP; Send race protection; duplicating V10 eligibility/CAS owners |
| V11 Automatic Unassign Chat Consequence (Slice 04; F05; BR-23, BR-26–BR-28, BR-47, BR-51, BR-54, BR-55; TX-04) | `backend/src/services/application.service.js` (`automaticallyUnassignApplication` wraps per-Application `A → NONE` + awaiting-assignee SYSTEM Message via shared `createSystemMessageIfConversationExists` / `SYSTEM_MESSAGE_CONTENT.AWAITING_NEW_ASSIGNEE` in one transaction when Conversation exists; batch callers remain independent per Application); `backend/src/services/application-chat-authority.service.js` (`evaluateApplicationConversationChatAuthority` for eligibility-loss window / PAUSED_UNASSIGNED / BR-54 LOCKED-TERMINATED denial; exported through application service) | Trusted V10 Automatic Unassign callers only (CompanyMember LOCK/TERMINATE, Recruitment Team removal, Platform Admin User LOCK/TERMINATE); not a public HTTP surface | Creating Conversation or Message when none existed; exposing lifecycle/team-removal reasons in SYSTEM Message; synthesizing Application replacement or A→B; global all-or-nothing lifecycle transaction for many Applications; waiting for `A → NONE` before denying Send on eligibility loss; Chat send/read HTTP; Company-lock freeze / terminal read-only surfaces; duplicating V10 Automatic Unassign CAS owners |
| V11 Conversation History Read + Authorization Modes (Slice 05; F02 read, F04/F05/F07/F08/F09 read modes; BR-07–BR-12, BR-16–BR-17, BR-22, BR-31–BR-40, BR-48, BR-51–BR-52, BR-54, BR-55) | `backend/src/services/application-chat-authority.service.js` (`evaluateApplicationConversationChatAuthority` ACTIVE / PAUSED_UNASSIGNED / ELIGIBILITY_LOSS_WINDOW / FROZEN_COMPANY / READ_ONLY); `backend/src/services/application.service.js` (`getCandidateApplicationConversation`, `getRecruiterApplicationConversation`); `backend/src/services/company.service.js` (`resolveRecruiterChatHistoryContext` — ACTIVE Recruiter without requiring Company operational); `backend/src/middlewares/authorize-recruiter-chat-history-access.js` | Candidate routes after Candidate authorization (`GET /api/candidate/applications/:applicationId/conversation`); Job My Applications routes after Recruiter Chat-history authorization (`GET /api/jobs/my-applications/:applicationId/conversation`) | Persisting Conversation mode/participant list/historical-assignee authorization; deriving authority from Message history; granting Primary/Supporting/CM/Platform Admin/Former Assignee Chat read; requiring Company operational or current Recruitment Team for frozen/terminal historical read; NORMAL Message Send / TX-06–TX-08 / realtime / notification / attachment |
| V11 NORMAL Message Send + Full Chat Concurrency (Slice 06; F02 send, F07–F10 send gates; BR-13–BR-14, BR-25, BR-32, BR-34, BR-39–BR-46, BR-49–BR-50, BR-54; TX-06–TX-08) | `backend/src/services/application.service.js` (`sendCandidateApplicationConversationNormalMessage`, `sendRecruiterApplicationConversationNormalMessage`, `commitNormalMessageSend` — reuses `evaluateApplicationConversationChatAuthority` + V10 TX-02 acquires Company → CompanyMember → User → Job team + Application writable CAS without bumping `version` or mutating business content; server-owned `NORMAL` sender identity); `backend/src/middlewares/validate-send-conversation-normal-message.js` | Candidate routes after Candidate authorization (`POST /api/candidate/applications/:applicationId/conversation/messages`); Job My Applications routes after Recruiter business access (`POST /api/jobs/my-applications/:applicationId/conversation/messages`) | Parallel Chat-authority / participant / locking model for Message; client-declared `type`/sender identity or SYSTEM Message create; mutating Conversation ownership / Recruitment Status / Assignment State / Candidate / Job / source / `submittedCvSnapshot` on Send; rewriting historical Message senders; changing Slice 05 read rules; inventing Assignment History, persisted Conversation status, realtime, Socket.IO, notification, attachment, read receipt, typing, online/last-seen, edit/delete/reaction, or Direct Chat |
| V13 Durable Chat Notification (Slice 04; F07 partial; BR-10, BR-34–BR-37, BR-47–BR-49; TX-01) | `backend/src/services/application.service.js` (`commitNormalMessageSend`, `createSystemMessageIfConversationExists`, and Assign-again via `firstAssignApplication` — persist each successful V11 Message plus its required `CHAT_MESSAGE_CREATED` NotificationEvent through `notification.service.js` in the active source transaction; recipient/content snapshots derive only from the trusted post-transition Application/Assignee state; best-effort post-commit materialization for NORMAL and assignment SYSTEM Messages that own their commit boundary) | Existing V11 NORMAL Message send and canonical assignment/Automatic-Unassign SYSTEM Message flows | NotificationEvent/Notification persistence, idempotent materialization, and recovery policy outside `notification.service.js`; client-declared sender/candidate/assignee/recipient authority; Assignment Notification types; Message/Notification/Conversation realtime; Conversation creation for Notification; Message read/delivery receipts, Socket session/delivery persistence, or reverse Notification references on Message |
| V12 Candidate Availability first submit + Application read projection (Slice 01; F01/F02/F04/F11 partial; BR-01–BR-10, BR-33, BR-35) | `backend/src/models/candidate-availability.model.js` (one current document per immutable `applicationId`, IANA timezone, `date + dayPart` slots, revision foundation); `backend/src/services/application.service.js` (`submitCandidateAvailabilityFirstTime`, Availability projection embedded only in existing Application reads) | Candidate Application route after access/Candidate authorization (`POST /api/candidate/applications/:applicationId/availability`); existing Candidate, Recruiter My Applications, and Primary/CM Application read projections | A second Availability model/workflow; a persisted Availability status or Application reference field; standalone Interview authority, cross-Application/tenant lookup, or Conversation/Message authority changes |
| V12 Interview Proposal + Pipeline Cutover / Reproposal (Slice 02, Slice 05; F05/F09; BR-11–BR-18, BR-20, BR-25–BR-26, BR-35–BR-36, BR-39; TX-01 foundation) | `backend/src/models/interview-schedule.model.js` (immutable proposal identity and active partial uniqueness); `backend/src/services/application.service.js` (`createInterviewProposal`, coupled Application/Availability/Schedule transaction) | Recruiter Job Application route after canonical access and Recruiter authorization (`POST /api/jobs/:jobId/applications/:applicationId/interview-proposals`) | A separate proposal workflow; independent `CONTACTED → INTERVIEW_SCHEDULED` Pipeline mutation; expiration runtime; Notification/realtime or Conversation/Message authority changes |
| V12 Candidate Availability edit + Availability↔Proposal concurrency (Slice 03; F03; BR-06, BR-08, BR-17; TX-02 + TX-01 completion) | `backend/src/services/application.service.js` (`editCandidateAvailability` — transaction with expected Availability `revision`, no `PROPOSED`, current-set replace via shared slot normalization, and revision advance; serializes with `createFirstInterviewProposal` through shared Availability revision CAS); `backend/src/middlewares/validate-edit-candidate-availability.js` | Candidate Application route after access/Candidate authorization (`PUT /api/candidate/applications/:applicationId/availability`) | A parallel Availability edit workflow; first-submit create-or-upsert on edit; mutating Application status or Schedule history on edit; Confirm/Decline/Cancel; reproposal; declined-slot exclusion; expiration runtime; notification/realtime; Availability history collection; Conversation/Message authority changes |
| V12 Candidate Confirm / Decline Interview Proposal (Slice 04; F06/F07; BR-13, BR-19, BR-21–BR-22, BR-24, BR-26–BR-27, BR-38) | `backend/src/services/application.service.js` (`confirmCandidateInterviewProposal` / `declineCandidateInterviewProposal` via one Candidate-ownership and `PROPOSED` + `expiresAt` guarded Schedule transition); `backend/src/models/interview-schedule.model.js` (immutable historical proposal identity) | Candidate routes after access/Candidate authorization (`POST /api/candidate/applications/:applicationId/interview-proposals/:interviewScheduleId/confirm` and `/decline`) | Recruiter Cancel; expiration mutation; terminal Application coupled cancellation; reproposal; Schedule-history read expansion; Notification/realtime; Availability history; Conversation/Message authority changes |
| V12 Recruiter Cancel + Schedule History (Slice 05; F08 partial/F11; BR-14, BR-20, BR-28, BR-33–BR-37) | `backend/src/services/application.service.js` (`cancelRecruiterInterviewProposal` guarded by current eligible Assignee; Application read projections hydrate current Availability and immutable Schedule history) | Recruiter Job Application cancel route after canonical access and Recruiter authorization (`POST /api/jobs/:jobId/applications/:applicationId/interview-proposals/:interviewScheduleId/cancel`); existing Candidate, current-Assignee, and Primary/CM Application reads | Recruiter `CONFIRMED → CANCELLED`; automatic expiration mutation; terminal Application coupled cancellation; independent Interview read authority; Availability history; Notification/realtime or Conversation/Message authorization changes |
| V12 Automatic Interview Proposal Expiration (Slice 06; F08 partial; BR-14, BR-20, BR-25, BR-28, BR-38) | `backend/src/services/application.service.js` (`expireDueInterviewProposals`, `expireDueInterviewProposalsForApplication(s)` — guarded atomic `PROPOSED → CANCELLED` when `now >= expiresAt`; operation-boundary invocation from V12 scheduling mutations and Application read hydration; no Application/Availability mutation) | Existing V12 scheduling mutations and Application read surfaces that already own Interview Schedule context | General-purpose scheduler/cron/TTL as the sole correctness path; status `EXPIRED`; Schedule hard-delete/TTL delete; terminal Application coupled cancellation; Recruiter `CONFIRMED → CANCELLED`; Notification/realtime or Conversation/Message authorization changes |
| V12 Terminal Application active Schedule cancellation (Slice 07; F08 partial; BR-29, BR-38–BR-39; TX-03) | `backend/src/services/application.service.js` (`cancelActiveInterviewScheduleForTerminalApplication`, called within the existing terminal Recruitment Pipeline and Withdraw transactions; guarded `PROPOSED`/`CONFIRMED → CANCELLED`) | Existing canonical Application terminal-transition workflows only | A separate terminal-Schedule lifecycle, cancelling historical `DECLINED`/`CANCELLED` records, resetting Availability, terminal-state rollback, general Recruiter `CONFIRMED → CANCELLED`, or Notification/realtime/Conversation authority changes |
| V12 Assignment + Interview read compatibility (Slice 08; F10/F11; BR-30–BR-36) | `backend/src/services/application.service.js` (existing `firstAssignApplication`, `reassignApplication`, `unassignApplication`, and automatic-Unassign owners retain Availability/Schedule identity; `hydrateCandidateMyApplicationViews` / `hydratePrimaryJobApplicationViews` project Interview data only after their existing Application-read authorization) | Existing Assignment / Reassign / Unassign workflows; existing Candidate, current-Assignee Recruiter, and Primary/CM Application reads | Resetting Availability/Schedule on Assignment change; treating Schedule creator as current mutation authority; granting Interview reads outside Application read authority; expanding Conversation/Message authority; a parallel Assignment or Interview-read workflow |
| Replace Submitted CV / PT-02 / BR-30 (V9 S04) | `backend/src/services/application.service.js` (`replaceSubmittedCv`) | Candidate application controller after canonical access authentication and Candidate authorization | Reimplementing Replace workflow outside `application.service.js`; duplicating `replaceSubmittedCv` in routes/controllers/middleware; adding snapshot history/logs; mutating Application business identity fields; speculative downstream pipeline persistence |
| Withdraw Application / PT-03 / BR-03 (V9 S05) | `backend/src/services/application.service.js` (`withdrawApplication`) | Candidate application controller after canonical access authentication and Candidate authorization | Reimplementing Withdraw workflow outside `application.service.js`; duplicating `withdrawApplication` in routes/controllers/middleware; adding snapshot history/logs; mutating Application business identity fields; speculative downstream pipeline persistence |
| CandidateCV persistence model (V7) | `backend/src/models/candidate-cv.model.js` | Candidate CV services and approved database tooling | Separate Generated/Uploaded collections; Location/EmploymentType/WorkMode collections; Company/CompanyMember/Job/Application/JobInvitation ownership or references on CandidateCV; speculative Candidate Search indexes; treating `archivedAt` as a `status` value |
| CandidateCV My CVs list/detail (V7 F02) | `backend/src/services/candidate-cv.service.js` (`listOwnActiveCandidateCvs`, `getOwnActiveCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel My CVs read outside the candidate-cv service; including archived CVs in the active library; authorizing via `PUBLIC` visibility, Company, Recruiter, Platform Admin, anonymous, or URL-knowledge access |
| CandidateCV create Generated Draft (V7 F03) | `backend/src/services/candidate-cv.service.js` (`createGeneratedDraftCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Generated-Draft create outside the candidate-cv service; accepting client ownership/Company/Recruiter/Job fields; requiring ACTIVE completeness at create; synthesizing Harvard content from metadata; auto-activating or setting Default on create |
| CandidateCV Generated Builder save + completeness (V7 F04 Builder) | `backend/src/services/candidate-cv.service.js` (`saveOwnGeneratedContent` / `saveOwnGeneratedDraftContent`, `evaluateGeneratedCvCompleteness`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Builder save outside the candidate-cv service; requiring every incomplete Education/WorkExperience/Project to be complete for DRAFT save; auto `DRAFT → ACTIVE` from Builder save alone; syncing Profile or metadata from Generated content |
| CandidateCV Generated activation + ACTIVE lifecycle (V7 F04 activation) | `backend/src/services/candidate-cv.service.js` (`activateOwnGeneratedCandidateCv`, `saveOwnGeneratedContent`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel activation/ACTIVE-edit outside the candidate-cv service; activating incomplete content; auto-activating on completeness without explicit activate; leaving `ACTIVE` with incomplete content; persisting `DRAFT` + `isDefault=true`; inventing Default set/switch/unset, archive, upload, Preview/PDF, or Profile/metadata mutation from activation/ACTIVE edit |
| CandidateCV Uploaded PDF business validation (V7 F05/F06) | `backend/src/services/candidate-cv-uploaded-pdf.service.js` (`inspectUploadedCandidateCvPdf`); limits in `backend/src/constants/candidate-cv-uploaded-pdf.js` | Candidate CV upload/replace workflows | Putting Uploaded-CV PDF rules (actual PDF, password, 10 MB, 20 pages) into generic `file.service.js` or trusting client `mimeType`/size/page metadata alone; relying on global `MAX_FILE_SIZE_MB` as the Uploaded-CV size authority |
| CandidateCV create Uploaded CV (V7 F05) | `backend/src/services/candidate-cv.service.js` (`createUploadedCandidateCv`); Uploaded storage policy in `backend/src/constants/candidate-cv-uploaded-storage.js` | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Uploaded-CV create outside the candidate-cv service; persisting `UPLOADED/DRAFT`; accepting client ownership/Company/Recruiter/Job/storage metadata; creating CandidateCV before PDF validation succeeds; storing Uploaded PDFs with public Cloudinary `upload` delivery; inventing replace/Default/archive/Preview/Harvard workflows on create |
| CandidateCV replace Uploaded PDF (V7 F06) | `backend/src/services/candidate-cv.service.js` (`replaceOwnUploadedCandidateCvPdf`); PDF inspection via `inspectUploadedCandidateCvPdf`; Uploaded storage policy in `backend/src/constants/candidate-cv-uploaded-storage.js` | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Uploaded-PDF replace outside the candidate-cv service; mutating ownership/`sourceType`/`status`/`name`/`visibility`/Category/ExperienceLevel/preferredLocations/skillTags/employmentTypes/workModes/`isDefault`/`archivedAt` on replace; committing mixed old/new `uploadedFile` metadata; replacing before PDF validation; cleaning up the old external file before persistence succeeds; rolling back a committed current-file swap when old-file cleanup fails; deleting a storage artifact that is still the CandidateCV current file after a concurrent/stale replace; storing replacement PDFs with public Cloudinary `upload` delivery; inventing Uploaded file history/`previousFiles`/versioning, Default/archive/Preview/rename, or Generated replace |
| CandidateCV rename / metadata / visibility update (V7 F07) | `backend/src/services/candidate-cv.service.js` (`updateOwnCandidateCvMetadata`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel metadata-update workflows outside the candidate-cv service; rewriting Generated Harvard content or Uploaded PDF from metadata; mutating `sourceType`/`status`/`isDefault`/`archivedAt`/ownership/Company/Job fields; inventing derived public-access fields (`isSearchable`, `effectiveVisibility`, `isPubliclyAccessible`); treating `PUBLIC` as anonymous/Recruiter/Company Manager/Platform Admin access or Candidate Search in V7; auto-activating/deactivating Generated CVs from metadata; creating catalog collections or Skill Catalog ownership in V7 |
| CandidateCV fixed Harvard PDF rendering (V7 F08) | `backend/src/services/candidate-cv-harvard-pdf.service.js` (`renderHarvardCandidateCvPdf`); section vocabulary in `backend/src/constants/harvard-cv-section.js`; embedded Unicode fonts under `backend/assets/fonts/noto-serif/` | Candidate CV preview/official-download workflows | Parallel Harvard/PDF renderers outside this owner; multiple templates; Canva/free-layout/AI rendering; persisting Generated PDF artifacts, versions, or history as canonical CandidateCV state; lossy ASCII/`?` sanitization of valid Unicode `generatedContent` to work around StandardFonts WinAnsi limits |
| CandidateCV Preview / Download (V7 F08) | `backend/src/services/candidate-cv.service.js` (`previewOwnCandidateCv`, `downloadOwnCandidateCv`); Generated PDF via `renderHarvardCandidateCvPdf`; Uploaded bytes via `file.service.js` (`downloadFileBuffer`) using Candidate Uploaded storage policy | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Preview/Download outside the candidate-cv service; authorizing via `PUBLIC`, anonymous, Recruiter, Company Manager, Platform Admin, or URL knowledge; returning Cloudinary/public URLs as the V7 access model; exposing `uploadedFile.storageKey` in Candidate-facing HTTP serialization; fetching Uploaded PDFs as public `upload` delivery when the domain policy is restricted; persisting `publicUrl`/`publicDownloadUrl`/`isPubliclyAccessible` or Generated PDF state; official PDF download of Generated `DRAFT`; Preview that mutates `status`/completeness/metadata/visibility/Default/archive; inventing Default/archive/Builder/upload/replace from Preview/Download |
| CandidateCV Default management / TX-01 (V7 F09) | `backend/src/services/candidate-cv.service.js` (`setOwnCandidateCvAsDefault`, `unsetOwnCandidateCvDefault`); maximum-one via CandidateCV partial unique index on active Default | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Default workflows outside the candidate-cv service; adding `User.defaultCandidateCvId` or Default history; auto-selecting Default on create or after unset/loss of eligibility; setting Default on Generated `DRAFT`, archived, foreign, or non-ACTIVE CVs; non-atomic switch that can leave zero Defaults mid-failure; inventing Archive (F10), Apply-with-Default, Candidate Search, Application, or Job Invitation from Default management |
| CandidateCV Archive (V7 F10) | `backend/src/services/candidate-cv.service.js` (`archiveOwnCandidateCv`) | Candidate CV controllers after canonical access authentication and Candidate authorization | Parallel Archive outside the candidate-cv service; hard-deleting CandidateCV or external Uploaded PDF on archive; inventing `status=ARCHIVED`, restore/unarchive, archive history/version collections; auto-selecting Default replacement; mutating ownership/`sourceType`/`status`/`visibility`/metadata/`generatedContent`/`uploadedFile` beyond atomic `archivedAt` + `isDefault=false`; leaving `archivedAt != null` with `isDefault=true`; inventing Candidate Search, Application snapshot, or Job Invitation from Archive |
| Company persistence model | `backend/src/models/company.model.js` | Services and approved database tooling | A second `Company` schema/model or direct use from routes/controllers/middleware |
| CompanyMember persistence model | `backend/src/models/company-member.model.js` | Services and approved database tooling | A second `CompanyMember` schema/model or direct use from routes/controllers/middleware |
| Job persistence model (V5–V6) | `backend/src/models/job.model.js` | Job service and approved database tooling | A second `Job` or `RecruitmentTeam` schema/model, duplicating Recruitment Team persistence outside the Job schema, replacement V4 catalog collections, or direct use from routes/controllers/middleware |
| Job Recruitment Team persistence and read boundary (V6 F01) | `backend/src/models/job.model.js` (embedded team fields); `backend/src/services/job.service.js` (team-read workflow) | Job controllers and approved database migration tooling | A separate RecruitmentTeam collection/model; a parallel team-read workflow outside the Job service; reusing generic V5 Job-content visibility to grant Supporting Recruiters broader content access; speculative audit/history/notification persistence |
| Recruiter create Job DRAFT (V5 F01) | `backend/src/services/job.service.js` (`createDraftJob`) | Job controllers | Parallel Job-create workflows outside the job service; treating client `companyId` as tenant authority; requiring submit-complete content at create time |
| Recruiter edit Job DRAFT (V5 F02) | `backend/src/services/job.service.js` (`updateDraftJob`) | Job controllers | Parallel DRAFT-edit workflows outside the job service; mutating ownership/creator/Primary/status/`publishedAt` through content edit; allowing non-Primary or non-DRAFT content mutation |
| Internal Job visibility boundary (V5 F03) | `backend/src/services/job.service.js` (`buildInternalJobVisibilityFilter`, `listInternalJobs`, `getInternalJob`) | Job controllers and later Company Staff Job workflows that must reuse the same visibility scope | Parallel internal Job list/detail authorization outside the job service; authorizing via creator or historical Primary alone; treating Job id as tenant authority; expanding into Candidate/public discovery semantics |
| Recruiter submit Job DRAFT for approval (V5 F04) | `backend/src/services/job.service.js` (`assertJobReadyForApprovalLifecycle`, `submitDraftJob`) | Job controllers and later approve/publish revalidation | Parallel submit workflows outside the job service; applying submit completeness at create/edit time; mutating content/ownership/creator/Primary during submit; non-atomic status transition that can leave partial lifecycle state |
| Company Manager Job approval-decision authority (V5 F05 / BR-20) | `backend/src/services/job.service.js` (`assertCompanyManagerJobApprovalAuthority`) | Later approve/publish and reject workflows (F06/F07) | Parallel CM approval-authority checks outside the job service; inventing review/marker state (`UNDER_REVIEW`, `reviewedAt`, approval-request entities); duplicating the F03 internal Job read path solely for pending review |
| Company Manager approve and publish Job / TX-01 (V5 F06) | `backend/src/services/job.service.js` (`approveAndPublishJob`, `assertJobPrimaryRecruiterValidForLifecycle`, `assertJobReadyForApprovalLifecycle`) | Job controllers | Parallel approve/publish workflows outside the job service; introducing an intermediate `APPROVED` state or separate publish step; mutating content/ownership/creator/Primary on approve; persisting `PUBLISHED` without `publishedAt` or `publishedAt` while still `PENDING_APPROVAL` |
| Company Manager reject pending Job / TX-04 (V5 F07) | `backend/src/services/job.service.js` (`rejectPendingJob`) | Job controllers | Parallel reject workflows outside the job service; inventing persisted `REJECTED` / rejection-metadata / soft-delete fields; deleting Jobs that have left `PENDING_APPROVAL`; cascading deletes into Company, CompanyMember, Category, or ExperienceLevel |
| Lifecycle-specific pre-publication Job hard-delete / TX-04 (V5 F12) | `backend/src/services/job.service.js` (`deletePrePublicationJob`, `assertPrePublicationDeleteAuthority`) | Job controllers | Parallel manual-delete workflows outside the job service; inventing `DELETED` / `isDeleted` / `deletedAt` / soft-delete fields; hard-deleting `PUBLISHED`/`CLOSED`/`EXPIRED`; granting Company Manager `DRAFT` hard-delete; granting Recruiter hard-delete outside own current-Primary `DRAFT`; authorizing delete via historical creator/former-Primary alone; cascading deletes into Company, CompanyMember, Category, or ExperienceLevel; duplicating a second persistence abstraction solely to distinguish reject from manual delete |
| Company Manager reassign Job Primary Recruiter / TX-03 (V5 F08) | `backend/src/services/job.service.js` (`reassignPrimaryRecruiter`, `assertCompanyManagerPrimaryReassignmentAuthority`, `assertRecruiterMembershipValidForJobPrimary`) | Job controllers | Parallel Primary-reassignment workflows outside the job service; mutating creator/company/content/status/`publishedAt`; reassigning non-effectively-`PUBLISHED` Jobs; assigning cross-tenant or inactive Primary; authorizing via creator/former-Primary/Job id/client `companyId`; mutating CompanyMember or inventing Primary-history / Supporting Recruiter documents |
| Manual close published Job / TX-02 (V5 F09) | `backend/src/services/job.service.js` (`closePublishedJob`, `assertManualCloseJobAuthority`) | Job controllers | Parallel close workflows outside the job service; mutating ownership/creator/Primary/`publishedAt`/content on close; closing non-effectively-`PUBLISHED` Jobs; hard-deleting or reopening `CLOSED`; authorizing via creator/former-Primary/peer Recruiter/Job id/client `companyId`; inventing `closedAt` or Job-history documents |
| Job effective expiration + public eligibility (V5 F10, F11 / TX-02) | `backend/src/services/job.service.js` (`resolveEffectiveJobStatus`, `isJobEffectivelyPublished`, `isJobPubliclyEligible`, `expirePublishedJobIfDue`, `findOutstandingPrimaryResponsibility`) | Later public/Candidate workflows and Job lifecycle processing that must reuse the same deadline semantics | Parallel deadline/effective-state or public-eligibility rules outside the job service; treating persisted `PUBLISHED` as still recruiting after deadline; inventing scheduler/cron/TTL as the sole correctness path; implementing Candidate discovery/search/Application solely to host the predicate; hard-deleting or reopening `EXPIRED`; mutating ownership/creator/Primary/`publishedAt`/content on expire |
| Recruiter outstanding Primary Job responsibility guard (V5 BR-41) | `backend/src/services/job.service.js` (`assertNoOutstandingPrimaryResponsibility`, `findOutstandingPrimaryResponsibility`) | Recruiter lock/terminate workflows | Reimplementing the outstanding-Primary query outside the job service; treating persisted `PUBLISHED` past deadline as still blocking; expanding TX-04/TX-05 into a jobs↔company_members multi-document transaction solely for this guard |
| Company membership / Company-role resolution (V3 F01) | `backend/src/services/company.service.js` (`resolveCompanyStaffMembership`, `findCompanyManagerMembership`, `resolveOwnedCompany`) | Auth, company, platform-admin, and approved middleware consumers | Parallel Company-role or tenant-membership resolution outside the company service |
| Company Staff tenant resolution and business authorization (V3 F02, F14, F15) | `backend/src/services/company.service.js` (`resolveCompanyStaffTenant`, `assertCompanyStaffBusinessAccess`, `resolveCompanyStaffBusinessContext`, `resolveCompanyManagerRecruiterManagementContext`, `resolveRecruiterBusinessContext`) | Approved middleware and Company Staff / Recruiter business services | Parallel tenant derivation from client `companyId`/`managerUserId`, or reimplementing layered business-access gates outside the company service |
| Company Staff business-access HTTP authorization | `backend/src/middlewares/authorize-company-staff-business-access.js` | Company Staff business routes after access authentication | Duplicating F14 layered checks in controllers, routes, or ad-hoc middleware |
| Company Manager recruiter-management HTTP authorization | `backend/src/middlewares/authorize-company-manager-business-access.js` | Company Manager Recruiter-management routes after access authentication | Duplicating BR-06/BR-24 role gates outside the approved middleware/service owners |
| Recruiter business-access HTTP authorization (V5) | `backend/src/middlewares/authorize-recruiter-business-access.js` | Recruiter Job routes after access authentication | Duplicating Recruiter role/tenant gates outside the approved middleware/service owners |
| Company Manager create Recruiter / TX-01 + activation issuance (V3 F03, F04) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel Recruiter-create or activation-token issuance workflows outside the recruiter service; including SMTP in TX-01; exposing bootstrap password or raw activation token to Company Manager |
| Company Manager list/detail Recruiter (V3 F08, F09) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel Recruiter list/detail workflows outside the recruiter service; authorizing from client `companyId`/`managerUserId`; exposing passwordHash, token, or session credential fields |
| Company Manager initiate Recruiter password reset (V3 F07) | `backend/src/services/recruiter.service.js` | Recruiter controllers | Parallel CM-initiated Recruiter password-reset issuance outside the recruiter service; creating a CM-specific token type; exposing raw reset tokens or passwords to Company Manager; treating password reset as unlock/restore |
| Company Manager lock/unlock Recruiter (V3 F11, F12 / TX-04; V6/V10 responsibility resolution) | `backend/src/services/recruiter.service.js` (`lockRecruiter` / `unlockRecruiter`; V10 extends lock with V6 Job-team transfer + Slice 06 Application automatic Unassign before completion) | Recruiter controllers | Parallel Recruiter lock/unlock workflows outside the recruiter service; changing User.status or Company lifecycle; restoring revoked AuthSession on unlock; unlocking platform-restricted Users or TERMINATED membership; completing lock while outstanding Primary Job or non-terminal Application responsibility remains; requiring Application replacement or synthesizing A→B Application handoff |
| Company Manager terminate Recruiter / TX-05 (V3 F13, F16; V6/V10 responsibility resolution) | `backend/src/services/recruiter.service.js` (`terminateRecruiter`; V10 extends terminate with the same V6 Job-team + Application automatic Unassign / final guard as lock) | Recruiter controllers | Parallel Recruiter termination outside the recruiter service; hard-deleting User/CompanyMember; changing User.status or Company lifecycle; releasing email for reuse; restoring TERMINATED membership to ACTIVE/LOCKED; completing terminate while outstanding Primary Job or non-terminal Application responsibility remains; requiring Application replacement or synthesizing A→B Application handoff |
| Candidate/Recruiter self password-reset issue and TX-03 completion (V1 + V3 F06) | `backend/src/services/auth.service.js` | Auth controllers; Recruiter service for shared issuance | Parallel PASSWORD_RESET issuance/completion outside the auth service; changing User.status, CompanyMember, or Company lifecycle during reset |
| V3 TX-07 Company Manager → Company Staff cutover | `backend/src/database/migrations/v3-tx07-company-manager-to-company-staff.js` | Canonical migration runner and focused migration tests | Embedding TX-07 transformation in services, models, startup, or seeds |
| Company Manager registration / TX-01 onboarding (V2 F01) adapted by V3 TX-06 | `backend/src/services/auth.service.js` | Auth controllers | Parallel Company Manager registration workflows outside the auth service |
| Company Manager confirm approval / TX-03 activation (V2 F07) | `backend/src/services/auth.service.js` | Auth controllers | Parallel confirmation-consume or Company/CM activation workflows outside the auth service |
| Recruiter activation completion / TX-02 (V3 F05) | `backend/src/services/auth.service.js` | Auth controllers | Parallel Recruiter-activation-consume workflows outside the auth service; changing User.status, CompanyMember, or Company lifecycle during activation; exposing raw activation tokens |
| Company draft profile and tenant ownership (V2 F02) | `backend/src/services/company.service.js` | Company controllers | Parallel Company draft/tenant resolution workflows outside the company service |
| Company submit and immutable review snapshot (V2 F03) | `backend/src/services/company.service.js` | Company controllers | Parallel Company submit/snapshot workflows outside the company service |
| Company Manager resend approval confirmation (V2 F08) | `backend/src/services/company.service.js` | Company controllers | Parallel approval-confirmation resend workflows outside the company service |
| Company Manager active Company profile management (V2 F09) | `backend/src/services/company.service.js` | Company controllers | Parallel active Company profile get/update workflows outside the company service |

## Consumption rules

- Consumers import the canonical owner; they do not reconstruct its state or configuration.
- A convenience re-export may expose an owner, but it does not become a second owner.
- Constants representing one domain concept have one canonical module.
- Services may access models directly under the current architecture.
- A repository/data-access layer must not be introduced unless explicitly approved as an architecture change.
- When an ownership decision changes, this document and the architecture documentation must change with it.

## Current known mismatches

The following items describe the audited repository. They are not target patterns and do not propose a remediation.

### The model barrel is disconnected and empty

[`backend/src/models/index.js`](../../backend/src/models/index.js) exports an empty frozen object. Concrete model modules such as [`backend/src/models/user.model.js`](../../backend/src/models/user.model.js) are imported directly by services rather than through the barrel.

Whether model barrels should be mandatory, optional, or removed is not established by the target conventions. The mismatch recorded here is that the existing barrel presents no usable model ownership or aggregation.

### The middleware barrel is empty

[`backend/src/middlewares/index.js`](../../backend/src/middlewares/index.js) is empty. Middleware consumers currently import concrete files directly. The target conventions do not establish whether a middleware barrel is required.

### Controller error formatting overlaps centralized error handling

[`backend/src/controllers/file.controller.js`](../../backend/src/controllers/file.controller.js) directly constructs error responses for a missing upload, a missing public ID, and a Cloudinary “not found” result. [`backend/src/middlewares/error-handler.js`](../../backend/src/middlewares/error-handler.js) separately owns the centralized error response structure for forwarded errors. The current code therefore has overlapping ownership of expected error formatting.

Controllers are still responsible for translating service outcomes into HTTP semantics under the target convention. The precise boundary between controller translation and centralized response formatting remains a human decision.

### Required configuration values also have unreachable defaults

[`backend/src/config/index.js`](../../backend/src/config/index.js) first rejects absent values in its required-variable loop, then supplies defaults for several of those same values. Examples include `NODE_ENV`, `PORT`, `MONGODB_SERVER_SELECTION_TIMEOUT_MS`, `JWT_EXPIRES_IN`, `JWT_ALGORITHM`, `BCRYPT_SALT_ROUNDS`, `SMTP_PORT`, and `MAIL_FROM_NAME`. When these variables are absent, validation throws before the defaults can be used.

### Input and business validation ownership overlaps

The file controller checks that a delete `publicId` is a non-empty string, and [`backend/src/services/file.service.js`](../../backend/src/services/file.service.js) repeats the same check. Upload existence is checked in the controller, while content type is checked in middleware. The repository does not yet apply a uniform distinction among transport validation, normalization, and business validation.

### Placeholder barrels and database seed files have no active responsibility

The middleware barrel and model barrel do not aggregate their layers. [`backend/src/database/seed.js`](../../backend/src/database/seed.js) is empty, and `backend/src/database/seeds/seed-1.js` contains only a placeholder comment. These files currently represent structure rather than active behavior.

## Decisions still requiring human confirmation

The target conventions do not settle these points:

- whether model and middleware barrel files are required public entry points, optional conveniences, or unwanted;
- the exact boundary between controller-owned HTTP error translation and centralized error-body formatting;
- which configuration values are truly required and which are intended to have defaults;
- whether request-shape validation belongs in route middleware, controllers, or a consistent combination of both; and
- whether seed scaffolding is intended to remain and, if so, what module owns seed orchestration.

Until those decisions are made, code must not establish a second owner or infer a new architectural layer.
