const JOB_STATUS = Object.freeze({
  DRAFT: "DRAFT",
  PENDING_APPROVAL: "PENDING_APPROVAL",
  PUBLISHED: "PUBLISHED",
  CLOSED: "CLOSED",
  EXPIRED: "EXPIRED",
});

const OUTSTANDING_PRIMARY_JOB_STATUSES = Object.freeze([
  JOB_STATUS.DRAFT,
  JOB_STATUS.PENDING_APPROVAL,
  JOB_STATUS.PUBLISHED,
]);

// BR-33 / F12: hard-delete is allowed only before publication.
const PRE_PUBLICATION_DELETABLE_JOB_STATUSES = Object.freeze([
  JOB_STATUS.DRAFT,
  JOB_STATUS.PENDING_APPROVAL,
]);

// BR-37 / F03: Company Manager internal visibility begins at PENDING_APPROVAL.
const COMPANY_MANAGER_INTERNAL_VISIBLE_JOB_STATUSES = Object.freeze([
  JOB_STATUS.PENDING_APPROVAL,
  JOB_STATUS.PUBLISHED,
  JOB_STATUS.CLOSED,
  JOB_STATUS.EXPIRED,
]);

export {
  COMPANY_MANAGER_INTERNAL_VISIBLE_JOB_STATUSES,
  OUTSTANDING_PRIMARY_JOB_STATUSES,
  PRE_PUBLICATION_DELETABLE_JOB_STATUSES,
};

export default JOB_STATUS;
